# Remaining preview UI acceptance

Authoritative 2026-09-12 closure: U01/U02/U04 are accepted in
`U01_U02_U04_RENDERED_ACCEPTANCE.md` after exact integrated source and fixed/CI
test retention. Their earlier unfinished wording below is historical.
U03/U07 remain blocking; U08 is now accepted after exact rendered-test and
fixed/CI retention. The U07 lane demonstrated inadequate input borders
and a collapsed 320-pixel goal layout; private minimal CSS repairs improve the
measured boundaries and geometry. The corrected focused case independently
passes in 15.634 seconds, with zero skips/guard violations/source drift/home
files/bytecode. Current page matched its accepted baseline before the two CSS
rules were overlaid. Four CSS geometries, control/text contrast, actual native
focus pixels, drawer keyboard/reflow and reduced motion are accepted for
integration. Form borders measure 4.422:1 against their fill; all measured
horizontal-content scrollers are absent after repair. The helper's two failed
focus-measurement attempts remain retained. The corrected sampler includes the
visible white inner stroke shown by retained image pixels without reducing the
3:1 assertion or changing runtime focus. Native browser zoom now independently
passes at 200%/400% in 13.762 seconds with fixed native bounds and full-size
screenshots; see U07_NATIVE_ZOOM_ACCEPTANCE.md. Its test integration/retention
is assigned to the conductor. Assistive-technology and native mobile-keyboard
evidence remain absent. The contrast/reflow test is already retained in fixed
workspace_ui/accessibility commands and CI.

Native Android feasibility, 2026-09-12: an installed API 34 Google APIs x86_64
image, Emulator 36.5.11, Platform Tools 37.0.0 and AVD creation tools are present.
One bounded metadata/help pass required no downloads and launched no server,
device, browser or IME. A fresh owned native-Android test may be possible, but
private ADB discovery/registration, guest egress and the full owned configuration
namespace are not yet established. Bootability and actual browser/IME presence
remain unverified. Do not reuse personal devices/AVDs, change host policy or
turn component presence into mobile qualification. The feasibility grant is
complete; its private report is retained and unchanged help need not be rerun.

U08's new real-browser writer-loss/recovery case independently passes in
43.907 seconds with zero skipped cases, guard violations, source drift,
synthetic-home files or bytecode.
`U08_BROWSER_ACCEPTANCE.md` records actual generation fencing, retained
same-operation uncertainty, natural backoff/coalescing and explicit recovery.
Exact test/fixed/CI integration is verified and U08 is closed; actual supervisor
lease handoff is outside the accepted passive preview. The U07 page matches the
accepted text after newline normalization, and its new test exactly matches
accepted bytes with fixed workspace_ui/accessibility and CI retention.

Current 2026-09-12 checkpoint: the task-identity repair has two actual-host
regressions failing before and passing after. Lead rendered v3 with that repair
reaches focal typed details and authorized artifact content. Two fixture errors
(waiting for a deferred request and journal-label punctuation) were corrected;
the next run has two failures in 38.907 seconds, no skips or guard violations,
and unchanged owned source. Escape/focus recovery during a pending body read is
assigned for bounded diagnosis; unknown owner state is the correct unobserved
state and requires a fixture correction. With that fixture corrected, the lead
independently passed the terminal-guidance and pending-action authentication-
expiry/same-key recovery case in 59.623 seconds. No skips or guard violations;
owned source unchanged, synthetic home empty and no bytecode. After the minimal
focus repair, the focal-artifact case independently passes in 12.486 seconds
with the same controls. Disabling the pending body button had moved focus to
the document body; moving focus to the existing Close button first preserves
the drawer's Escape handler and opener return. The complete case covers typed
navigation, unauthorized-body refusal, authorized artifact content, deferred
response rejection and later task-scope filtering. Both cases are accepted for
conductor integration with their synthetic screenshots and actual toolchain.
Remaining delivery-state rendering has a separate bounded acceptance lane.

Remaining U04 worker states independently accepted for integration, 2026-09-12:
one real Chromium case covers accepted, queued, included, submission started,
submitted, acknowledged and proven not-submitted in 43.884 seconds, no skips or
guard violations, unchanged owned source and an empty synthetic home. Seven
individual state cards are captured; acknowledged and no-contact cards were
visually reviewed. Actual validated synthetic producers retain separate
contact/spend/ack, selected turn, scoped actions and bound journal timestamps.
No worker/provider executes. This complements the five terminal/held states in
the accepted canonical case; it does not qualify physical receipt delivery or
separate supervisor-inbox modes. Retain both new rendered tests in fixed/CI
coverage and verify integration before U04 closure.

2026-09-12 canonical rendered draft: the lead's independent v2 replay ran two
actual Chromium cases and returned two failures in 57.29 seconds, zero skips,
zero guard violations, unchanged shared/owned source, empty synthetic home and
no bytecode. The first reached canonical workspace/council/deliberation
navigation and truthful terminal labels, then found no current-task evidence
target with body availability. The second found the general identity guidance
instead of the expected selected-task identity fields. A bounded source-only
fixture/contract investigation is assigned; neither result is yet classified as
a product defect or an accepted end-to-end case. The earlier helper attempts
failed on CSP-disallowed fixture waiting and incomplete synthetic expiry proof;
those were corrected without changing page security. Preserve these failed
results; screenshots and complete rendered acceptance remain pending.

Source follow-up: initial login has no selected task, explaining the generic
identity copy and why the fixture missed focal filtering. A separate product
defect is confirmed: detail task IDs use the detail-schema HMAC domain while
view task IDs use the view-schema domain. The page compares them directly,
so selecting a task hides its own detail targets. Use one canonical task
presentation identity across both projections and server lookup; retain the
separate detail-item namespace and all server/body-scope checks. The conductor
owns integration of the correction and its real selected-task and wrong-task
regressions; the private repair is independently source-reviewed.
The fixture correction must expose this mismatch rather than avoid selection.

2026-09-12 correction checkpoint: four focused current-page/Node checks pass
independently (zero skips/errors/failures), covering privileged drawer cleanup,
focus, active scrim ownership, idempotent body loading and render-time linked
recovery. The first lead attempt stopped before Node due to a Windows audit-hook
argument-handling error; the corrected exact-command guard passed the same
selection. Source stayed unchanged and the synthetic home remained empty.
This is page-script evidence, not a rendered/browser or complete UI gate.
The actual-host typed-error tests now assert finite response-body error codes,
so a generic refusal with the same HTTP status cannot pass unnoticed.

The lead independently passed all ten current non-browser detail cases in
58.789 seconds, with zero failures/errors/skips, unchanged shared and copied
source, an empty synthetic home and no bytecode. The guarded run allowed only
owned loopback endpoints, prohibited child processes and external network, and
used synthetic canonical workspace, council and deliberation sources. It covers
typed refusal bodies, canonical source reads, separate body authorization and
declared state/revision drift. This does not close rendered U01/U02 acceptance.
The external canonical reader now reuses the payload whose state/revision was
validated, closing the separate validation-to-reread consistency gap. Eight
canonical actual-host cases independently pass in 33.900 seconds, including
council/deliberation between-read mutation and truthful decided, unresolved and
cancelled labels. There were no skips, failures, errors, source drift, home
writes or bytecode. The owned-loopback guard denied child processes and external
network. This closes the bounded reader correction, not rendered U01/U02/U04.

An additional in-flight expiry counterexample is independently reproduced:
start a separately authorized detail-body request, expire and clear the session,
then deliver the earlier successful response. The exact current page script
appends one body node after expiry even though the drawer stays hidden. Discard
stale detail/body responses using authentication and selection generations,
including expiry, close and new selection; retain a deferred-response regression.
The synchronous cleanup cases above did not cover this privacy defect. The
conductor's generation check now closes the reproduced late-body path: five
current page/Node cases independently pass in 0.706 seconds, including the
deferred-response regression, with zero skips/errors/failures and no shared
drift. This accepts the bounded source/script correction; final rendered and
broader UI requirements remain open.

Current source/review crosswalk, 2026-09-08. U01/U02/U03/U04/U07/U08 remain
required. Authentication is separately accepted under F02; ordinary host command
wiring and the complete disposition vocabulary are under F17_PUBLIC_COMMAND_BRIEF.md.

## Implementation before the final rendered pass

| Requirement | Concrete remaining work |
| --- | --- |
| U01 scoped review/decision navigation | Add actual detail/navigation views through the single local facade. Preserve typed source identity: workspace assessments, advisory council reviews and fixed-option deliberation decisions cannot be relabeled as each other. Authorize each scoped read/operation on the server; do not forward arbitrary bearer tokens or embed capability-bearing cross-origin links. Retain legacy entry points. Gated provider/ballot actions remain gated. |
| U02 focal task artifact/evidence inspection | The current shell exposes an evidence count only. Add a bounded separately scoped artifact/evidence list and detail projection with opaque identifiers and safe metadata. Raw receipts, private paths, hashes and bodies must not leak through ordinary navigation; body access needs explicit separate scope. Preserve the one-task focal area. |
| U04 terminal reasons and action guidance | All normative delivery labels already exist and are tested. Project safe finite reason categories for rejected, expired, cancelled and dead-lettered states, not just held-for-recovery. Show next permitted action or an explicit unavailable reason without releasing holds or implying retry. Keep contact/spend/acknowledgement separate. |
| U04 recorded event times | The outer journal already contains authoritative `ts`; `collect_snapshot` currently discards it and the page always says time unavailable. Pass allowlisted timing metadata bound to the same event identity/revision and coherent snapshot. Include it in snapshot/cursor identity. Display **Journal recorded at**, never provider submission or acknowledgement time. Do not add a writer or rewrite history. |

Timing metadata must have an exact numeric type, finite value and supported
range, and must match its event. Missing/invalid historical time stays unavailable;
do not substitute current browser time, revision or admission `observed_at_ms`.
Pure inner-envelope fixtures without timing metadata remain supported. Verify
actual producer-to-view association through filtering, pagination and reopen,
plus stale/mismatched timing refusal. The earlier source-only assertion that
timestamps were absent upstream was corrected by lead inspection of `_rundir`
and `_swarm_coordinator`.

## Focused rendered evidence after integration

### Current provider-free source checkpoint

The source now carries the outer journal frame timestamp through the read-only
projection without changing the signed inner event. `occurred_at` is emitted
only for a finite, bounded timestamp that matches the event operation key;
missing or invalid historical timing remains unavailable, and timing metadata
participates in the snapshot/cursor binding. The shell labels this value
**Journal recorded at** and never presents it as provider submission or
acknowledgement time. The provider-free HTTP journey now covers opaque browser
message send, a separately authenticated owner provisioning endpoint, conflicting
action/request refusal, browser command, and same-key lookup after fresh
authentication and reopen. Independent execution passed one journey in 5.49
seconds with 254 watched source files and the owned copy unchanged. Provisioning
uses the message operation key; private delivery IDs are not needed in its
request. This accepts the focused HTTP checkpoint, not every negative authority,
capacity or rollback case, retain/link behavior, or the final rendered/browser gate.

Independent timing checkpoint: nine timing/pagination/snapshot cases passed in
3.17 seconds in an isolated owned copy, with 254 watched source files and the
owned source unchanged. The actual page timestamp formatter also passed ten
JavaScript date-boundary and fallback assertions. This accepts the covered
timing behavior only; the remaining U04 requirements below remain open.

- U01/U02: actual public host navigation and focal-task artifact/review/decision
  detail, with scoped refusal and no secret-bearing links. An injected adapter
  alone does not prove the ordinary entry works.
- U03: actual mobile keyboard/visual-viewport geometry with composer, notices
  and pending state. Preserve the accepted 210-event pagination/anchor checks;
  do not repeat them unchanged unless the integration affects those boundaries.
- U04: every delivery boundary, safe reason and action guidance, recorded time,
  separate contact/spend uncertainty and absent model evidence.
- U07: measured remaining text/nontext/focus contrast, zoom/reflow, reduced
  motion and actual assistive-technology announcements with burst coalescing.
  Existing keyboard/focus/return and narrow-layout evidence remains accepted.
- U08: rendered pending-action recovery with unresolved contact/spend,
  authentication expiry and observed owner state. Preserve the accepted
  offline-hint/backoff, dialog and U14 owner-presentation behavior; distinguish
  synthetic observations from an actual ownership handoff.

Capture the exact browser/toolchain and source revision plus retained sanitized
rendered artifacts. Missing screenshots/toolchain evidence is not a demonstrated
runtime defect, but remains a release gate. Use owned synthetic state only;
no real rooms, providers, accounts or telemetry contents are needed.

The conductor owns scoped implementation and integration. Finish the public
entry/presentation gaps before the combined rendered pass. No additional
provider, installation, commit or release authority is granted.

U01/U02 preparation checkpoint: use host-established task/source bindings and
opaque identifiers for workspace assessments, workspace next-task decisions,
council advisory reviews and deliberation decisions. Install bounded scoped
list/detail reads through the ordinary host; canonical readers already exist.
Reject wrong task/run/type, stale bindings and revoked scope before source
resolution. A pending deliberation is not a completed decision. Never forward
the separate deliberation UI's capability-bearing URL.

Artifact metadata and item bodies need separate authorization; allow_text alone
must not grant body access. Keep bodies out of export and retained navigation
state. Extend the existing focal-area navigation and drawer focus handling for
interactive detail content. Pure review/decision reader tests remain component
evidence only; require actual ordinary-host HTTP and rendered keyboard journeys.

### U01/U02 provider-free implementation checkpoint

The ordinary loopback host now has an optional, finite `WorkspaceDetailAdapter`
for typed review, decision and workspace-evidence navigation. It accepts only
host-bound workspace/run/task/source-kind bindings and projects opaque target
and task identifiers. Metadata/detail reads use the authenticated local facade;
wrong task, wrong source kind, stale/revoked bindings and resolver state or
revision mismatches fail closed before source resolution. A separately
installed body scope is required for detail bodies; normal text scope does not
grant it, and body responses keep task identity opaque. The page exposes a
keyboard-accessible **Reviews and decisions** drawer that stays scoped to the
focused task. This checkpoint is provider-free implementation evidence, not a
claim that canonical council/deliberation producers or the final combined
rendered gate are accepted.

U04 implementation safety checkpoint: terminal delivery reasons are identifiers,
not a finite enum, in the current protocol validator. Do not project those raw
values to fill the UI gap. Map terminal states/reasons to bounded safe categories
with an unknown fallback, and reserve the Hold label for held context. Derive
permitted action guidance from authenticated capability projections; explanatory
text cannot confer retry or execution authority. Verify secret-looking valid
reason identifiers never appear in snapshots or rendered content, alongside
terminal-state labels and explicit unavailable guidance.

U04 terminal guidance packet independently verified before integration: 137
projection/focused tests passed in 1.11 seconds in a fresh owned copy, with 260
watched files unchanged. The actual embedded page script passed 34 standalone
synthetic DOM assertions with its three inputs unchanged. Source review accepts
finite terminal categories, held-only Hold labels and capability-derived guidance.
Conductor integration is authorized using additive hunks against current source;
ordinary-host and rendered acceptance remain separate required evidence.

Independent typed-detail checkpoint: four actual-host/detail cases, including
real Chromium keyboard navigation, passed in 12.93 seconds with shared and
copied source unchanged. Source confirms copied installation scopes, stale and
revoked binding refusal, exact resolver state/revision matching, opaque body
task identity and a distinct workspace-evidence type. This accepts the scoped
adapter checkpoint only: tests resolve synthetic generic records; canonical
council/deliberation readers and real focal artifact/evidence navigation still
need integration evidence before U01/U02 can close.
