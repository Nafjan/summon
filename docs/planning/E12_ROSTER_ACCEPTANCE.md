# E12 roster-drift acceptance

Status: verified for the 3.5 preview, 2026-09-12. Final runtime/test and metadata
retention are independently verified; earlier unfinished wording below is
historical. No provider, account,
installation, commit or release qualification follows from these local fixtures.

Current closure packet, 2026-09-12: the integrated durable approved-alias journey
independently passes alongside the public parser regression (two cases in 3.529
seconds). It seals real private continuation history, reserves/prepares/loads a
child, passes the unchanged role binding, then refuses a separately approved
alias remap without changing original source/result bytes. Fresh resolution
sees the replacement without acquiring continuation or ballot authority.

The final in-flight/model/effort test-only packet is independently accepted for
integration. Three guarded cases pass; no skips, guard violations, source drift,
home writes or bytecode. Source review showed the previous in-flight fixture
changed the second identity read before durable start. The corrected fixture
requires two unchanged reads and a third read after real turn_started, frozen
identity/history, and no launch-guard or process call. Model/effort changes are
bracketed by passing unchanged controls and preserve the complete pending claim
and all prior files. No runtime or qualification behavior changes.

After conductor integration, verify exact test retention and the accepted final
L02 structural mapping before closing E12. Existing replacement-name and
published-reader refusal evidence below remains retained; no new broad routing
or provider run is needed.

Final test retention, 2026-09-12: both integrated test files exactly match the
three-case independently tested packet. Fixed commands and CI retain both;
the worker additionally reports 106 focused cases plus four subtests passing.
The final L02 structural packet is the remaining closure dependency. Earlier
wording below describing the sealed-task alias journey as absent is historical.

2026-09-12 integration checkpoint: both replacement-binding runtime files match
the independently accepted packet exactly. The test differs only by independent
standard-library import order and a trailing blank line; its import set and
remaining AST match, and canonical/fixed-gate/CI retention is verified. The
conductor reports 36 current turn/fork passes and four roster cases. The new public dispatcher control proves
a fresh alias invocation refuses a target definition changed after approval.
It does not consume the previously frozen seat and therefore does not establish
approved alias remap against a stored task. Complete that existing boundary
through durable continuation-role binding and loaded-invocation validation;
preserve historical bytes and distinguish a separately requested fresh task.
Do not add alias support to chat or repeat unrelated routing suites.

Explicit replacement-binding packet accepted for integration, 2026-09-12:
the lead source-reviewed the recovered patch and independently passed 36
guarded cases in 4.850 seconds against a fresh current-source copy, with zero
skips/errors/failures, shared-source drift or bytecode. The trusted runtime API
freezes the selected replacement definition, derives its ceiling from recorded
history, refuses missing authority and incompatible permissions, preserves all
bindings through chained and multi-participant forks, and refuses ambiguous
lineage. Parent bytes remain unchanged and child creation launches nothing.

The published 3.4.0 reader uses schema 1 and strictly refuses a v2 room header;
the new payload fields are part of the unreleased v2 contract. Retain that
published-reader refusal and inventory the new shape under L02. Do not infer
compatibility with arbitrary intermediate development snapshots. Conductor
integration and remaining alias/task, in-flight drift and effort/model journey
reconciliation are still required before E12 closes. This is independent native
engineering acceptance, not Fable or live-provider certification.

## Earlier bounded fork evidence

The lead reviewed the complete portable guarded turn/fork test and independently
passed four cases in 1.22 seconds. The four relevant conversation, launch-guard
and source-family files matched current source. The helper's 108-file frozen
packet stayed stable; separate transport files changed in the shared tree.
Exact functional retention is verified in the current source, CI and release
manifest; only trailing blank lines differ from the reviewed test packet.

The actual journey uses `ConversationJournal.create`, `ConversationRuntime.start_turn`
and `fork_turn`. Owned synthetic history is explicitly unqualified; the source
family uses its real local producer without fabricated provider evidence. Rename
and removal refuse roster lookup; changed model refuses incompatible identity;
the unchanged control refuses missing qualification. Refusals preserve all prior
stored bytes and launch nothing. Explicit fork preserves the parent's exact
history prefix and creates a child containing the selected new prompt, with no
turn or contact. The model-change child resolves the new model definition.

Rename/removal forks preserve old participant membership. They therefore still
refuse the missing name: this proves preservation, not replacement-name binding.
The preview needs an explicit supported replacement-binding journey before E12
can close. A new binding must not silently rewrite the old room or authorize work.

| Requirement boundary | Evidence and remaining work |
| --- | --- |
| Catalog rename/removal/approved alias remap | `test_roster_drift_acceptance.py` freezes and revalidates actual definitions/role registry. A fresh freeze is not a task rebind. |
| Actual turn refusal and explicit fork | Approved four-case `test_e12_turn_fork_acceptance.py` packet covers the bounded unqualified-history journey above. |
| Drift after durable turn start | Existing `test_conversation_runtime.py` has synthetic worker revalidation fixtures; reconcile and retain their precise scope before closure. |
| Saved model/effort drift | Existing `test_job_resume.py` tests actual invocation validation. It does not itself establish an explicit new participant binding. |
| Workspace replacement instance | Existing simulated workspace demo preserves task/message lineage across incarnation replacement. This is not provider roster rename or alias admission. |
| Remaining release acceptance | Explicit replacement-name binding/fork, actual supported alias-to-task boundary, frozen in-flight definitions, unsupported effort and unverified model behavior through the relevant paths. Use authenticated synthetic fixture qualification only as local protocol evidence; live served-model claims remain separately gated. |

Do not repeat unrelated routing or provider suites to close these missing
journeys. The conductor owns any runtime changes; the lead owns acceptance.
