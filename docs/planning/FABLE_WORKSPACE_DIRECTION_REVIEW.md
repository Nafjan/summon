# Workspace integration review

Date: 2026-09-05. FABLE-WORKSPACE-01 completed successfully through Summon with authoritative reported matching served identity. Review input fingerprints were unchanged. Fable inspected the named source; the independent test execution belongs to the lead. No moving P1 protocol patch, release, installation or provider qualification was accepted by implication.

## Decisions

Latest bounded follow-up, FABLE-SUPERVISOR-02: APPROVE for the frozen simulated taskless context-receipt increment. The single read-only consultation completed with successful execution, a valid report, authoritative matching root `claude-fable-5-1` identity and eight unchanged packet files. Auxiliary Haiku usage was also reported; this is not an exclusive-model claim. Fable inspected source; the independent 903-test/61-subtest execution remains the lead's evidence. No provider/native, moving operator-control, UI or release acceptance follows.

| Follow-up | Lead disposition |
| --- | --- |
| SI02-01: omitted helper slices | Review scope limitation, not fresh implementation failure. The packet omitted demo/content/spawn and test-harness dependencies. Root independently inspected and tested the integrated path; include decisive helper slices in the next recovery packet. |
| SI02-02: replacement epoch across senders | Source confirms old-epoch checking currently skips other source tasks and does not first restrict to the endpoint. Preserve original SI-07: require explicit hold/disposition of old queued/offered deliveries from every sender at the same endpoint. Unrelated endpoints must not block. Core owner has two-source and cross-endpoint regression requirements. This strengthens the next recovery increment; it does not retroactively claim multi-source replacement was qualified. |
| SI02-03: exact-cap linked recovery | Required next real-journal recovery test. Construct a reachable history using valid transition order; linking must refuse without changing the journal or admit with all remaining parent/child/endpoint settlement fitting the reserve. Keep structural counts distinct from operational reachable capacity. |
| SI02-04: UI lineage absent from review | Fable did not certify UI; retain root's separately bounded source/test/rendered evidence and pending active-control gates. |
| SI02-05: actual linked successor | Still required. Runtime/core owners are implementing actual endpoint recovery, followed by ordinary task-attempt recovery; pure linked-record tests do not complete either journey. |

The lead accepts Fable's capacity evidence refinement for this bounded milestone and its source confirmation of INBOX-R01/R02/R03. Endpoint revision tests remain real journal/protocol evidence rather than a complete public-command maximum-boundary qualification. The lead does not adopt an unmeasured operational concurrency estimate from the review. No follow-up provider review is required before the assigned concrete implementation/tests.

Subsequent independent closure: SI02-02 and SI02-03 passed within the lead's 113-test/53-subtest gate, with 33 unchanged dependencies. The real journal test refuses a new-epoch offer while another sender's old same-endpoint delivery remains queued, then permits it after explicit hold. Unrelated endpoints are covered only by a pure guard because the preview still admits one endpoint. The capacity test measures reachable link admission, proves cap-minus-one refusal leaves the journal unchanged, and admits at the exact cap with parent disposal, child cancellation and endpoint retirement still fitting; inherited unknown exposure remains. These close the assigned bounded conditions. SI02-01/04 retain their review-evidence scope limits and SI02-05 still requires actual process recovery.

HANDOFF for the next review: carry SI02-01 through SI02-05 with the real linked-recovery evidence and decisive helper slices. Preserve uncertainty, immutable parent history, separately installed successor authority and full ordinary F18 requirements. Fable reported no files/resources left behind; the eight supplied files were independently verified unchanged.

- R02 Stage A: accepted by Fable and the lead at the inspected revision. F-01 owner-generation and F-02 explicit-lineage carry are closed. The lead independently verified 63 tests and two subtests with no failures, skips or source drift. F-04/F-05 remain separate follow-ups; authenticated qualification and full R01/R02 stay open.
- P1 direction: approved with the conditions below. The lead adopts the coordinator-owned goal/task/message/delivery model, private content sidecars, trusted-OS-user owned-channel qualification, and a conductor demonstration before the later chat bridge.
- R13 interface seam: approved in direction, with durability recovery semantics and prospective legacy behavior to settle before storage integration. Derived constants still need final constructor/path checks.

## P1 acceptance conditions

| ID | Required evidence |
| --- | --- |
| P1-C01 | Assessments bind current goal revision, known criterion IDs and resolvable event/artifact evidence; dangling references refuse and projections rebuild from events. |
| P1-C02 | Main iteration two binds an explicit decision referencing iteration one's assessment. Replay preserves causal order; repeated decision is idempotent; repeated admission cannot create another attempt. Main-one completion occurs while the side lane is active. |
| P1-C03 | Workspace events and pending delivery settlement use the coordinator's existing byte budget. Content blobs have separate count/byte/retention bounds. Budget classes must follow required recovery/control obligations, not merely event names. |
| P1-C04 | Normal old commands targeting the published workspace address must refuse at initial `_fence_run_files`, before acquire or torn-tail repair. New consumers validate manifest/layout before selecting inner state and give typed version refusal. Header-only or leading-feature-event-only protection is insufficient. This gate remains unimplemented/unqualified. |
| P1-C05 | Implement pure contracts, then coordinator/replay and capacity classification, then content, then IPC and the repeated conductor demo. The chat bridge follows that evidence. |

The lead refines P1-C03: ordinary goal/priority/assessment writes differ from delivery disposition that settles a required obligation. Fable's broad suggestion that disposition events are ordinary cannot override the reserved cancellation/recovery guarantee. Classify the concrete transition before implementation.

The lead refines P1-C04 after source inspection: old semantic replay rejects a new feature event, but old acquisition and repair can precede that rejection. The selected direction is a versioned outer layout with a manifest and structural nonregular guard at an old managed filename (candidate: a `generation.txt` directory), with actual inner state still owned solely by the existing coordinator. Validate the manifest/layout before selecting inner state and finish the guard before publishing the outer address. Test normal old mutations against valid-prefix journals with torn tails and require unchanged outer and inner bytes. The inner feature event remains a semantic negotiation mechanism, not standalone old-writer protection. No layout implementation or certification is claimed; old histories remain unchanged, and arbitrary same-user direct targeting of private inner state is outside this published-address guarantee. This bounded compatibility work supports the conductor demonstration without adding another store or scheduler.

## R13 advice requiring verification

R13-G01: uncertain append must not produce an idempotent durable-success response or free a settlement obligation. Fable suggested treating superseded segment tails as unproven and re-recording outcomes under a successor. The lead has not accepted that mechanism: current per-mutation owner acquisition and replay semantics may make a blanket rule demote normal completed writes or duplicate reducer effects. A dedicated storage specialist is checking the actual lifecycle and a minimal safe alternative. Distinguish proving current persistence from proving an earlier acknowledgement or external side effect.

R13-G02: apply the reserve prospectively to existing runs, retaining explicit capacity-degraded behavior when their remaining space cannot cover obligations. Avoid an unrelated new marker competing with P1 feature negotiation. Any durable-format change still requires real compatibility tests.

HANDOFF: R02's bounded source milestone is accepted. Continue isolated P1 contracts and resolve the single R13 durability/replay seam through its specialist lane. Do not broaden this into another full architecture review, approve guessed constants, or apply model advice without source verification. Full workspace and release gates remain open.

## FABLE-WORKSPACE-02: foundation review and lead disposition

The single bounded consultation completed successfully with APPROVE for the five preliminary protocol, replay, content, layout and transport layers. The lead inspected the result envelope: exact root `claude-fable-5-1`, authoritative `claude_root_response`, read-only permission and a valid report. Sixteen reviewed files remained unchanged during the review. Auxiliary Haiku usage was also listed; this is exact root-review identity, not an exclusive-model claim. Moving R13 code was a dependency observation and was explicitly not accepted. The reviewer reported an external harness plan artifact; it was not independently located or removed, and no claim of zero external artifacts is made.

| Finding | Lead disposition and next evidence |
| --- | --- |
| WS-01: replay views | Required. Facade replay must depend only on the journal prefix and any authenticated persisted observation time, never the current wall clock. Prove admitted history replays after lease expiry while live inspection shows expiry. Existing persisted-time derivation may satisfy this; test before rewriting it. |
| WS-02: settlement reserve | Required before any pipe write. Main R13 owner must reserve concrete workspace delivery obligations and atomically admit claim plus selection. Empty supported-event classes remain a refusal, not completion. |
| WS-03: affected-task holds | Required for the critical-side-finding demo. Protocol/replay lane implements typed affected tasks and unresolved-hold enforcement. A main-2 hold must block its decision and grant; an unrelated side-1 hold permits main progress. Never infer release from favorable prose. |
| WS-04: content placement | Facade lane must create the coordinator first, then provision content within the inner run; preserve the exact outer layout and old-command refusal. Resolve the constructor/provisioning order with an actual fixture. |
| WS-05: stream selection | Adopt. Whole-message selection must not skip an earlier unresolved queued message in the same stream. Independent streams remain eligible. |
| WS-06: late holds | Recheck holds at actual turn admission even if a next-lane decision was previously recorded. No automatic decision revocation or grant creation. |
| WS-07: initial priority | Adopt with WS-03: type the initial task reference and validate complete lane binding. This is an unreleased contract delta requiring independent review. |
| WS-08: receipt mapping | Preserve distinct observations: full pipe write is not submitted; authenticated frame receipt is the fixture submission boundary; acknowledgment means recipient-received only. Partial/unknown writes retain a hold. |
| WS-09: other old writers | Existing guard proves the coordinator entry path only. Use a dedicated workspace namespace, never silently share council/deliberation/room roots. Direct old `acquire_owner` and arbitrary inner targeting remain outside the claim; no universal old-writer protection is claimed. |
| WS-10: unavailable content | Add observation-backed missing/mismatch/unavailable hold reasons. Inventory or access failure must not be mislabeled missing, and no content error can erase prior submission uncertainty. |
| WS-11: private root | Provision and verify the dedicated private root before constructing the facade. Successful test fixtures alone do not establish production initialization. |

The lead assigned the bounded schema/replay delta and facade integration tests in parallel. R13 source review separately found three acceptance gaps: state/prefix identity binding, state-aware append/creation enforcement, and escaped-control byte maxima. Those corrections remain with the main developer. Passing helper tests or Fable's foundation approval does not close them.

HANDOFF: Complete these integration conditions, then demonstrate two real conductor iterations with main work progressing during a bounded side investigation. Independently review the schema delta and replay seam. No provider continuation, native attachment, hostile-user isolation, commit, installation or release approval follows from this review.

## FABLE-SUPERVISOR-01: taskless inbox decision

One bounded review completed with CONCERNS and approval of the additive typed-inbox direction. The lead verified a successful valid read-only report, exact requested/targeted/served `claude-fable-5-1`, authoritative `claude_root_response` evidence and an unchanged seven-file review snapshot. Auxiliary Haiku usage appears in model metadata; this is an exact root-review identity claim, not an exclusive-model usage claim. No retries, model/account fallback or release action were performed.

The lead adopts additive endpoint/message/inbox-delivery variants in the existing journal. A generic destination union would still require a distinct inbox lifecycle while changing qualified worker-execution validators. Share content/correlation validation, send identity/index, stream sequencing, envelope and reducer machinery; do not create a second framework. The lead inspected the cited task-only evidence registration and delivery-only reserve code, and independently verified the actual worker-origin path after the review packet was frozen.

| Finding | Lead decision and closure evidence required |
| --- | --- |
| SI-01: quotas and settlement | Required. Explicit inbox states, event classes, obligation tables and proof roles; combined 32-message/128-delivery limits across both modes; endpoint revocation/recovery capacity and exact encoded settlement reserve. Test the mixed 128 boundary and exact-cap receipt or hold/disposition without extra space. |
| SI-02: evidence subjects | Required. Exactly one task or endpoint evidence subject, scoped lookups and inbox offer/receipt binding. Refuse both subjects, dangling endpoints and grant substitution in both directions. |
| SI-03: consumer identity | Required. Resolve an actual installed consumer handle through a trusted callback under the owner mutation. JSON owner/epoch fields cannot authenticate. Historical readback still requires the original currently authenticated consumer binding; test wrong handle and stale/replaced epoch. |
| SI-04: uncertain exposure | Required, with the conservative late-receipt option. An expired or replaced consumer cannot record an accepted receipt through this endpoint. Preserve held/unknown exposure; refusal does not prove non-exposure. A linked successor inherits uncertainty and an explicit possible-duplicate marker naming its predecessor offer. Never reset offered history to not exposed or promise exactly-once recovery. A future late-observation contract is separate. |
| SI-05: parent direction | Adopt forward-only parents from the same source principal to the same supervisor endpoint for this inbox slice. Task-message and reverse-direction parents refuse until a corresponding typed supervisor-send operation exists. |
| SI-06: epoch prefix | Required. New-epoch offers wait until same-source old-epoch queued/offered deliveries have an explicit hold or disposition. Epoch replacement alone cannot skip unresolved context. |
| SI-07: generic event bypass | Current worker-to-worker refusal is independently verified in the actual authored-message test. Extend the invariant to the new inbox composite and actual consumer gate; current proof does not certify an unimplemented endpoint. |

Receipt qualification must say simulated and identify the consumer kind. It proves that the installed consumer received the bytes, not model context inclusion, understanding, human review, a council vote or a deliberation ballot. The actual child-to-supervisor gate must preserve task count, claims, priority, permissions and spend.

HANDOFF: revise the contract with these decisions, then implement strict protocol/core admission and actual consumer integration in separate owned files. Keep ingress inactive until independent quota/reserve, authority, crash/restart and process tests pass. The next Fable review receives the revised contract plus closure evidence for SI-01 through SI-07; no cosmetic review round or release approval is implied.
