# Leadership and review authority

Updated: 2026-09-05. The owner explicitly authorized the general product direction and goals, delegated implementation details to the product lead and main architect, and designated Fable via Summon as the principal consultant and advisor. The owner welcomes a slightly adversarial or skeptical review style to challenge the lead's decisions. The lead retains decision ownership; Fable supplies technical review and may second decisions. This clarifies the earlier instruction to seek Fable approval instead of repeatedly requesting owner authorization.

This supersedes the earlier per-slice authorization blockers in the planning records. The next P0 reliability slice may proceed through Fable technical approval, implementation by the developer and independent source/test verification by the lead. The full reviewed workspace roadmap remains the objective; incremental acceptance is not completion of that roadmap.

## Current role allocation

2026-09-12 next local evidence grant: the conductor's last observed turn is
completed and idle; no further provider review or shared integration is
dispatched. Native `release_windows_probe_review` may prepare a fresh private
one-case timing diagnostic that directly measures time inside the existing
audit callback, preserving every admission and denial. Remove the diagnostic
profiler, not the audit guard; use the unchanged existing demo test. Return the
reviewable diff before execution. One helper, no descendants, ten-minute
preparation limit. The lead may then authorize one bounded diagnostic execution.
Native `telemetry_producer_fix` may independently prepare complete canonical
`release_outcomes`, `model_catalog` and `schema_inventory` captures using the
existing hidden owned launcher and actual collector. Preserve all selected
cases, commands, source bindings and failure/skip policy. Preparation only,
fifteen minutes, no descendants; return exact unresolved process/read forms.
Both grants allow only fresh external private artifacts and source-only checks.
No shared runtime/test edits, account/provider contact, real installation, Git
mutation or publication. Source/planning freeze begins after this grant is
written and remains until the lead ends the resulting captures or preparation.
The lead independently reviews the UI draft during these bounded preparations.

2026-09-12 capture grants ended. Both full core attempts are terminal and
unqualified; complete recovery is independently accepted at 72/72. The single
bounded demo profile completed at one passed case with explicit attribution
limits. UI preparation and its independent Node syntax check are complete;
browser activation is not granted by those checks. All owned test/diagnostic
processes at this checkpoint have ended. Source/planning freeze ends for this
documentation checkpoint, which changes the full payload hash; preserve the
original evidence bindings. L09_CANONICAL_CAPTURE_CHECKPOINT.md records results
and the next measurement requirement. No provider, account, real installation,
Git mutation or publication authority is added. The conductor's separately
reported owner-directed review round is outside these finished grants.

2026-09-12 first canonical core attempt ended at its 600-second collector
deadline. The outer process exited in 615.332 seconds, with its owned child
reaped, the job handle closed and no cleanup errors. Source and Git facts were
unchanged. Private console progress contains 812 success markers out of the
prior 841-case collection, but no complete canonical outcome was produced;
these markers are not accepted passing-case evidence. Completed descendant
guard receipts contain no violations; the terminated collector has no final
guard receipt. Retain this failed attempt. The lead authorizes one complete
rerun with the same collector, commands, admissions and source bindings in a
fresh private execution, raising only the collector deadline to 1,200 seconds.
The observed progress supports a longer bounded run, not a test exclusion or
product timeout change. Freeze all source/planning again for this capture.
The conductor reports a separate owner-directed validation round; no recovery/UI
runner handoff or evidence is inferred from that activity.

2026-09-12 core capture execution checkpoint: the lead independently reviewed
the revised private launcher, unchanged admission prefix and identical prepared
source/toolchain configuration. Four private harness files passed syntax-only
checks. The lead now starts one complete fixed workspace_core capture with a
600-second collector deadline, hidden owned process tree and external outputs.
No deselection, skip waiver, provider contact or source mutation is authorized.
All shared-source and planning writes are frozen until this capture terminates.
This is instrumented local validation, not clean-release or publication evidence.

2026-09-12 canonical capture finishing grant: the lead accepts the prepared
complete-core design subject to its private cleanup/diagnostic correction.
Native `release_windows_probe_review` may make one fresh private revision and
perform source-only syntax checks; no test launch or shared edit is delegated.
The lead owns the first complete canonical core execution after final review.
The designated conductor may independently prepare a private recovery/UI
extension of the same launcher, retaining the fixed commands and actual
collector. Prepare exact synthetic child and owned-loopback/browser admissions
only; no test/server/browser launch, shared edit, provider/account contact,
installation, Git mutation or publication. One conductor, no descendants,
fifteen-minute preparation bound. Return the concrete extension and unresolved
forms for review. All shared-source and planning writes freeze when the lead
starts capture; later edits invalidate reuse of its full-payload source binding.

2026-09-12 parameter-ID integration grant ended with acceptance. All 151 selected
test files independently match the collected candidate after newline normalization;
the other 256 compared runtime/test/configuration files are unchanged. The
eighty-test patch is integrated without runtime or policy changes. The current
owned launcher-preparation grant below is the only unfinished native helper
work; no canonical runtime capture has started. Source freeze begins only after
the launcher is reviewed and the lead explicitly starts that bounded capture.

2026-09-12 L09 launcher preparation now has a narrower executable design grant
for native `release_windows_probe_review`: prepare one owned, reviewable guarded
capture of the complete fixed workspace_core suite, with no test execution yet.
Use unchanged release_gates._run_outcomes and its real collector subprocess.
An explicit temporary child-environment test seam may add only the owned audit
startup directory, existing dependency directory and fixed no-cache/no-bytecode
controls; preserve the production environment policy in shared source and record
this instrumentation. Do not change commands, ROOT, hashes, Git facts, results
or skip policy. Descendants must be admitted by exact executable, source,
arguments and synthetic environment where isolated mode prevents hook inheritance;
an audit hook is not an OS sandbox. Review the two fixed synthetic transport
modes and actual historical coordinator probe; refuse every unrecognized launch.
No general sandbox redesign, command rewriting, source edits, account/provider
contact, installation, Git mutation or publication. One helper, no descendants,
fifteen-minute preparation bound; return the actual runner and unresolved forms
for lead review. Guarded local validation is distinct from clean-candidate final
publication evidence and cannot waive that evidence.

2026-09-12 parameter-ID preparation grant ended. The lead independently verifies
eleven selections, 151 files, 330 decorators, 248 ID additions and eighty changed
tests. AST comparison preserves all non-ID logic and all existing IDs; collector
and policy source are unchanged. One owned guarded collection passes: 3,298
distinct union cases, all eleven actual selection mappings, zero identity or
collection problems, zero guards/source drift, and zero executed tests. Synthetic
unused Capture platform metadata cannot qualify a platform or release result.
The conductor may now integrate exactly the accepted eighty-test metadata patch,
preserving every unrelated edit, runtime and registry. Ten-minute boundary,
one integrator, no descendants or rerun of unchanged collection. Compare final
selected source to the reviewed candidate and stop on a semantic conflict.
No providers, accounts, installation, Git mutation or publication is authorized.

2026-09-12 native `release_windows_probe_review` may prepare one private launcher
for the existing canonical release collector against the settled shared source,
without executing tests yet. Reuse the accepted CE/VR/browser guard machinery;
source stays read-only and only fresh owned synthetic outputs/homes are writable.
Preserve canonical commands, collection/results, rendered artifacts and strict
qualification. Include exact bounded hidden child, dependency, historical Git
read and owned-loopback controls for core/recovery/UI checks; deny real providers,
accounts, installations, Git mutation and external network. No wrapper-produced
summary may replace canonical outcomes. Fifteen minutes, one helper, no
descendants or shared edits. Stop with the reviewable launcher and unresolved
execution boundaries; do not redesign a sandbox or expand the release scope.

2026-09-12 final packet integration grant ended with independent acceptance of
all eighteen selected paths and the broader 335-file source/configuration
comparison. The conductor remains the sole shared integrator. A new native
`telemetry_producer_fix` grant may prepare an owned-copy test-only correction
for missing or unsafe explicit parameter IDs in pytest files selected by the
current fixed suite/gate registries. Preserve every parameter value, mark,
fixture, assertion, test body and selected case; add reviewed finite literal IDs
without changing collector/runtime/privacy policy. Existing valid IDs remain.
No shared edits. Return the exact changed-test inventory, source bindings and
an AST comparison with only parameter-ID metadata removed. At most two guarded
collection-only attempts may check actual case identity through the existing
collector, clearly distinct from executed tests or passing release evidence.
One helper, no descendants, twenty minutes; owned synthetic files and hidden
bounded Python only, no provider/network/private account state, installation,
Git mutation or publication. Stop on unresolved dynamic cases rather than
weakening collection or silently excluding them. Lead reviews; conductor lands.

2026-09-12 recovery checkpoint accepted: the designated conductor integrated
VR-07; both selected files independently match the reviewed composition after
newline normalization. The initial two-file grant has ended. Extend the same
recovered native conductor run to the remaining five accepted patches only:
VR-01/02, VR-05/06, corrected VR-03/04 v2, CE-01 through CE-05, and the runtime
regression-retention increment. Apply CE before retention; preserve VR-07 and
all unrelated changes. This covers sixteen additional selected runtime/test/CI
and inventory paths. No redesign or new files beyond the accepted patches is
granted. One integrator, no descendants, fifteen-minute boundary. Return actual
integration and selected-source comparison evidence; stop on a semantic conflict.
The lead independently verifies the combined candidate. All existing provider,
account, installation, Git-mutation and publication restrictions remain intact.

2026-09-12 bounded conductor recovery: after repeated unsupported completion
acknowledgments and an unchanged shared target, the lead authorizes one native
Astra/high recovery turn in the existing designated conductor task. This is
necessary execution recovery under the owner's limited-Astra instruction, not
a new advisory cycle or a change of conductor ownership. Requested host settings
are not authoritative served-model evidence. The first checkpoint is exactly
the accepted VR-07 runtime delta and its regression file: inspect, integrate
only those reviewed changes, and return a verifiable diff. Preserve unrelated
runtime, planning and roster edits. No new helpers, provider calls, account or
credential operations, installation, staging/commits or publication are granted.
Stop at that checkpoint or a specific execution limitation; the lead verifies
the shared result before granting the remaining accepted integrations.

2026-09-09 the seven-context grant has delivered and ended; the lead independently
accepted 173 metadata tests in 17.73 seconds, unchanged owned source. Next grant
to native `l02_stable_authority`: owned-copy metadata for exactly ten fleet
literals: fleet-plan, fleet, fleet-activation-binding, fleet-dispatch-anchor,
fleet-dispatch-claim, fleet-dispatch-initialization, fleet-dispatch-ledger,
fleet-dispatch, fleet-launch-evidence and fleet-terminal-projection (all v1).
Use the accepted context artifact as baseline. Extend existing entries where
only a producer/reader reference is missing; do not duplicate a family merely
to reduce lexical gaps. Preserve private authority/evidence and live gates.
Owned tools/format_inventory.py and metadata tests only; public source/fixture
reads and at most two source-only checks, twenty minutes, hidden ninety-second
children with telemetry/autoload/bytecode/cache disabled. No runtime execution,
shared edits, private state, providers, installs, commits or descendants. End on
incremental patch and evidence, for lead review and conductor integration.

2026-09-09 next bounded L02 grant: native `l02_stable_authority` may prepare
an owned-copy additive metadata patch for exactly the seven context-compiler
families: context-input, context-lineage, context-output, context-receipt-binding,
context-receipt-proof, context-reference-proof and context-rollback (all v1).
Use the accepted reader-contract-v2 artifact as baseline, preserving all prior
changes. Read public producer/consumer and fixture source only; declare actual
authority, readers or explicit detached output, and precise evidence gaps.
Modify only owned tools/format_inventory.py and metadata tests; no runtime or
shared edits, private state, provider/network contact, installs, commits or
descendants. Two source-only test attempts maximum, twenty minutes, hidden
ninety-second children with telemetry/autoload/bytecode/cache disabled. End on
patch and evidence. The conductor alone integrates after independent review.

2026-09-09 current checkpoint: all `l02_stable_authority` grants below have
delivered and ended, including the four-family patch, producer-only design,
F17 contract challenge and owned reader-contract-v2 implementation. The lead
independently accepted the latter artifact with 167 metadata tests in 15.65
seconds, unchanged owned source. These artifacts await conductor integration;
completed helper grants authorize no further work. The designated conductor
remains active on F17 public controls and retains sole shared integration.

2026-09-09 `l02_stable_authority` may prepare an owned metadata/tests patch
for four stable families only: operator message grant, operator scope,
supervisor offer observation and supervisor receipt observation. Preserve
authority versus observed evidence and exact current source/fixture references.
Do not change/map active F17 command policy/request/decision interfaces.
One helper, two source-only test attempts, no descendants/shared/runtime edits,
private state/providers/network/install/commit/release. Stop at additive patch
and compatibility gaps; conductor retains integration.

2026-09-09 `l02_cumulative_packet` grant ended. The final owned pair preserves
all five accepted artifacts, deduplicating only the identical protocol allowlist
replacement. Lead independently verified exact added/removed-line reconciliation.
One combined source-only run passed 123 tests in 9.18 seconds, unchanged owned
Python source. The conductor has the final pair and cumulative patch; canonical
integration and any additional conductor acceptance tests remain to be checked.

2026-09-09 `l02_cumulative_packet` may consolidate the five accepted metadata
artifacts (three-batch combined, telemetry, durable, wire, reviewed stable
contracts) into one owned final tools/test pair and cumulative patch. Preserve
every accepted entry/test; deduplicate only the identical `protocol` allowlist
change. No new mapping or semantic redesign. One helper, no descendants,
one source-only combined check; no shared/runtime edits/private state/providers/
network/install/commit/release. Stop at artifact, exact-content reconciliation
and evidence. Conductor alone integrates.

2026-09-09 `l02_public_contracts` grant ended. The lead corrected the mixed
launch-scope family to private evidence with an explicit distinct public-result
shape, then independently passed 74 source-only tests in 3.49 seconds. The
verification copy includes the already accepted usage assertion correction
missing from the helper's partial integration baseline. Owned Python sources
were unchanged during the run. The conductor has the reviewed additive patch;
no runtime or private-state evidence is claimed.

2026-09-09 `l02_public_contracts` may prepare an owned metadata/tests patch
for at most eight stable input/capability families: resume capability v2,
resume launch scope, workspace plan v1, workspace admission, operator-send
base, supervisor-inbox base, worker-send base and supervisor-offer budget.
No producer-only projection mapping or validator relaxation is granted.
One helper, two source-only test attempts, no descendants/shared/runtime edits,
private state/providers/network/install/commit/release. Stop at accurate
producer/consumer/fixture handoff; conductor alone integrates.

2026-09-09 durable/wire grants ended. The lead source-reviewed both packets
and independently passed 86 source-only tests in 3.19 seconds (six durable/
nested families) and 74 in 1.78 seconds (seven wire/spawn families), with owned
Python sources unchanged. Both add the same explicit `protocol` discriminator
allowance; the conductor must integrate that line once. All compatibility gaps
remain explicit; no runtime fixture or provider ran.

2026-09-09 the read-only `l02_new_schema_audit` grant ended with a 29-family
classification. No family was established as only a hash-domain label; nested
records and in-process contracts remain distinct from independent stores.

New `l02_new_schema_audit` durable-mapping grant: owned metadata/tests patch
for at most six families (background launch v2, nested accounting handoff,
workspace v1/v2, economics reservation and effects observation). New
`l02_wire_mapping` grant: owned metadata/tests patch for at most seven families
(worker/supervisor pipe, context, message request/result, transport admission
and transport budget). Each has two source-only test attempts, no descendants,
no shared/runtime edits or private state/providers/network/install/commit/
release. Preserve already accepted metadata batches; stop at patches and
honest producer/consumer/fixture gaps. Conductor alone integrates.

2026-09-09 `l02_new_schema_audit` may perform a read-only public-source audit
of newly introduced schema literals absent from format inventory, compared
with the verified local 3.4.0 historical source candidate. Return a finite
producer/consumer/fixture classification and integration order; distinguish
wire/durable/public records from internal hash domains. No metadata patch,
runtime imports, private state, tests, providers/network/install/commit/release
or descendants. One helper; stop at source-backed handoff.

2026-09-09 `l02_numeric_binding` consolidation grant ended. One combined
source-only run passed 107 tests in 3.55 seconds with shared baselines unchanged.
The lead independently verified that the combined patch's added/removed lines
exactly match the three accepted packets. The conductor has one integration
artifact; this does not close broader L02 compatibility.

Historical consolidation-only grant:
combine the three already accepted metadata patches in a fresh owned public
source copy, preserving exact accepted content. No redesign, new mappings or
shared edits. One source-only format/schema check, then one combined patch and
conflict report; stop rather than silently resolve semantic conflicts. No
runtime tests/providers/private state/network/install/commit/release/descendants.

2026-09-09 `l02_telemetry_mapping` grant ended. Four numeric format records
were source-reviewed and independently passed 74 source-only format/schema
tests in 3.68 seconds, with owned Python sources unchanged. No telemetry module
or runtime fixture was imported. The conductor has the accepted patch.

Historical grant: `l02_telemetry_mapping` may prepare an owned-copy metadata patch
limited to format inventory and its focused tests for telemetry configuration,
historical v1 events and current v2 events (at most four records). Read public
producer/reader/fixture source only; do not import telemetry or inspect its
state. Preserve permissive-reader limitations explicitly rather than changing
runtime or claiming strict version enforcement. One helper, no descendants,
two source-only test attempts, no shared edits/providers/network/install/commit/
release. Stop at patch and evidence handoff; conductor alone integrates.

2026-09-09 `l02_numeric_binding` and `l02_usage_mapping` grants ended. The lead
source-reviewed both owned patches and independently passed their complete-copy
format/schema checks: 107 tests in 2.63 seconds and 73 tests in 2.18 seconds,
respectively, with owned Python sources unchanged. Earlier copy/collection
failures were harness omissions, not accepted passes. No runtime fixture ran.
The conductor has both packets for combined integration and verification.

Historical grant: `l02_usage_mapping` may prepare an owned-copy metadata-only patch to
`tools/format_inventory.py` for usage snapshot and usage-live private store,
anchor, checkpoint, key binding, runner plan and public projection families.
Owned `tests/test_format_inventory.py` may also replace the stale assertion
that the now-mapped usage family remains unmapped with positive mapping evidence
and an actual remaining gap; preserve unmapped-family visibility checks.
At most eight records, one native helper, no descendants and two source-only
test attempts. Inspect public source and fixture definitions only, never usage
stores or credentials. No runtime changes, shared edits, providers, network,
installs, commits or release. Stop at source-backed patch and gap handoff;
the conductor alone integrates after lead review.

Historical grant: `l02_numeric_binding` may prepare an owned-copy patch limited to
`tools/format_inventory.py` and `tests/test_format_inventory.py`: represent
literal supported-version set membership without executing source, map the
historical process-v1 reader honestly, and test strict rejection boundaries.
One native helper, no descendants, at most two source-only verification runs;
stop at patch and evidence handoff. No shared edits, runtime behavior changes,
private state, providers, network, installs, commits or release. The conductor
retains integration; the lead reviews the contract change independently.

2026-09-09 `l02_inventory_batch` grant ended. Five source-reviewed metadata
records passed the lead's independent 73-test source-only check in 2.34 seconds,
with owned Python sources unchanged. The conductor has the accepted patch;
process-v1 representation and broader L02 closure remain open.

Historical grant: `l02_inventory_batch` may prepare one owned metadata-only patch for
supported historical conversation/process v1 formats and fleet approval/activation
formats, at most twelve records. Read public producers/readers and existing
fixtures; never inspect runtime stores. Do not add fictional producers/readers,
turn metadata checks into runtime certification, or erase evidence gaps. Only
owned copies of format inventory metadata may be edited; no shared files or
runtime behavior changes. Existing source-only format/schema tests may run with
plugin autoload and telemetry disabled. No providers/network/private state,
installs/commits/releases or nested delegation. Stop at a source-backed patch
and concise coverage/remaining-gap handoff; conductor retains integration.

2026-09-09 L04 helper grants are ended. After reviewing the exact loopback-only
fixture correction, the lead independently passed the full ordinary/Kimi/junction
owned rehearsal in 105.117 seconds. Only exact canonical test retention remains
for this acceptance handoff. All writes/replacements were synthetic test state;
real installations, providers and release actions remain reserved.

2026-09-09 L04 diagnostic grant ended after two ordinary-root failures; the
second isolated HTTPServer's loopback reverse-name lookup as the denied boundary.
The lead reviewed the fixture-only exact `127.0.0.1` to `localhost` substitution
around `host.start`; all other names and DNS/connect operations remain denied.
`rollback_acceptance_review` may apply that owned-packet correction and make
one further ordinary-root attempt, retaining sanitized diagnostics, all guards,
source-drift reporting and cleanup. No shared edits or real installation. Stop
at the result; the full three-branch gate remains required and unverified.

2026-09-09 `rollback_acceptance_review` may diagnose the owned L04 seed-worker
failure with at most two ordinary-root diagnostic attempts, within fifteen
minutes. Keep the reviewed packet/guards and exact LF-or-CRLF marker correction;
add only sanitized exception-class and source basename/function/line diagnostics,
never raw errors, locals, keys or private paths. All source/state/processes remain
in owned synthetic copies/destinations; no shared edits, real installation,
providers/network/accounts/spend, commits or publication. Preserve and report
source drift. Stop with the classified failure and bounded proposed correction;
do not widen guard authority or weaken the rollback assertions for a pass.

2026-09-09 bounded L04 harness correction: the first independent owned rehearsal
reached successful current-payload verification in all three path branches but
failed the driver's LF-only acknowledgement assertion against Windows CRLF.
The lead may accept exactly either LF or CRLF for that fixed success marker in
the owned proposed packet and rerun the same guarded test. No runtime assertion,
authority boundary or real installation scope changes.

2026-09-09 rollback test authoring and its bounded installed-reader correction
are complete; the helper grant is ended. The lead reviewed the packet and parsed
both Python files without execution. The conductor may integrate the proposed
test-only files and perform a guarded rehearsal confined to owned synthetic
destinations under existing test authority. Real managed installation remains
reserved; failure must be classified without widening guards or weakening
assertions for a pass.

2026-09-09 `f17_host_wiring` ended without edits or execution after identifying
the missing explicit command-authority interface. The lead source-checked the
finding and recorded the separate bounded host command-policy decision in
F17_PUBLIC_COMMAND_BRIEF.md. The conductor may implement that in-scope interface
and its acceptance tests under existing integration authority; no new provider,
account, installation or release authority is granted.

2026-09-09 `f17_host_wiring` may author an isolated proposed patch for the F17
ordinary-host connection to existing typed operator commands, plus focused
actual-entry tests. Writable scope is only its owned temporary copy of
`_workspace_entry.py` and corresponding workspace-entry tests. It may inspect
related public runtime/plan/command contracts read-only. No shared edits, test
execution, new command semantics, schema/routing/permission expansion, providers,
private state, installs, commits or publication; no nested delegation. Derive
finite eligible targets/actions from explicit host authority, never prose or
membership, and preserve the single journal owner. Stop at a reviewable patch
or identify the exact extra interface needed. This is a partial F17 connection,
not full F17 acceptance; the conductor owns integration and remaining CLI and
disposition vocabulary, and the lead owns review.

2026-09-09 `rollback_acceptance_review` receives a new bounded test-authoring
grant: create a proposed L04 composed rollback test patch and concise handoff
only in its own temporary directory. Reuse the reviewed public fixtures and
pin the locally verified historical source candidate inside the proposed
harness. No shared repository edits, test/installer execution, real managed
state, network/providers, accounts, spend, installation, commits or publication;
no nested delegation. The patch must confine all future state and source
replacement to owned synthetic roots and preserve hidden-child/cleanup guards.
Stop at the reviewable patch or a concrete prerequisite; the lead reviews
execution safety and the conductor alone integrates shared files.

2026-09-09 `rollback_acceptance_review` completed its read-only handoff; its
grant is ended. The lead independently verified the historical source candidate
and key reusable reader/test references. No tests or installs were run. The
composed test remains unimplemented/unverified and belongs to the conductor's
remaining L04 queue.

2026-09-09 `rollback_acceptance_review` has one bounded read-only L04 packet:
inspect public source/tests and local Git reference availability, then propose
the smallest composed owned rollback acceptance covering encrypted pending
state, retention keys, authority/uncertainty preservation and supported paths.
No edits, test execution, network, installed/private state, providers, spend,
installs, commits or publication. No nested delegation. The grant ends with
the concise source-backed handoff; the conductor retains runtime integration
and the lead retains acceptance. This native helper does not supply Fable approval.

2026-09-09 parent-chat fixture clarification and ten-minute extension: the
reviewer may reuse the exact previously reviewed synthetic-profile command
admission adaptation for one owned marked command script. Actual observer,
guard ownership, model checks, prompt/attempt binding and accounting/journal
writers remain intact. General command/authority bypass is forbidden. Record
the test-only profile-admission limitation; it does not certify a provider.
All existing preconsumer, hidden-child, cleanup and private-state restrictions
remain in force. Finish the packet or report the precise safe failure.

2026-09-09 the migration reviewer has fifteen minutes for an owned test-only
parent chat economics lifecycle and interrupted-evidence packet. Preserve real
start/handoff/finish/reopen authority, preconsumer fences and honest synthetic
qualification limits. Only exact owned hidden/cleaned children are allowed;
no shared edits, private state, providers, spend or reserved actions. If the
safe real path is unavailable, report the seam instead of substituting a mock.
The conductor owns fixes/integration; the lead retains acceptance.

2026-09-09 the migration reviewer has twelve minutes for read-only E07
requirement-to-evidence reconciliation across current public source, retained
tests and accepted checkpoints. Distinguish missing evidence from implementation
gaps, preserve full preview scope, and do not reopen superseded findings. No tests,
processes, edits, private state, providers or reserved actions. The lead retains
acceptance authority and reviews remaining context/usage semantics locally.

2026-09-09 the migration reviewer has fifteen minutes to prepare an owned
test-only R15 rotation-after-child-validation case from the exact approved
synthetic reproduction. Preserve actual authentication and claim writers,
preconsumer fences, uncertainty and unchanged-claim assertions. No shared or
runtime edits, private state, providers or reserved actions; only an existing
exact owned fixture runner may execute if required, hidden and cleaned up.
The conductor owns implementation and integration after E07.

2026-09-09 the economics reviewer has ten minutes for read-only independent
review of the corrected encoded-capacity bound, complete admitted shapes,
producer compatibility and numeric-width documentation. No tests, processes,
edits, private state, providers or reserved actions. The conductor owns fixes;
the lead reviews contract implications and accepts evidence separately.

2026-09-09 completed grant recorded: the migration reviewer had fifteen minutes
for an owned, guarded copy of the two v2 economics demo journeys. Only reviewed
fixed Python workers, hidden children and synthetic environment were permitted;
no shared runtime edits, private state, providers or reserved actions. The helper
reports two passes and clean child shutdown. Independent lead packet review is
pending; simulated contact states do not establish live provider evidence.

2026-09-09 the economics reviewer has a ten-minute read-only delta check of the
new bound-accounting validator, duplicate settlement, encoded capacity bound and
actual plan/runtime callers. No tests, processes, shared edits, private state or
provider/reserved actions. The lead independently reviews and runs the frozen
four-case actual chat dispatcher/guard packet under its existing owned-CLI grant.

2026-09-09 the migration reviewer has fifteen minutes for an owned test-only
actual chat dispatcher/executor/v2-guard acceptance packet. Reuse the marked
synthetic CLI and preconsumer fences; only exact owned executables may run,
hidden and cleaned up. Verify actual matching input and changed prompt/attempt
refusal, with no installed CLI/provider, account/credential, private state,
shared/runtime edit, installation, commit or release. The conductor owns fixes.

2026-09-09 a native reviewer has ten minutes of read-only review of the new
workspace economics settlement writer, its tests and actual runtime callers.
Check bound outcomes, idempotence, capacity, owner fences and uncertainty without
tests, shared edits, processes, private state, providers or reserved actions.
The lead independently reruns the approved guarded migration packet on an owned
copy of current public source, without modifying the shared runtime.

2026-09-08 after the read-only v2 review, the same migration reviewer has fifteen
minutes to adapt the existing owned numeric-version packet to supported v1/v2,
future 99, mixed headers/events, cancellation ordering and explicit legacy fork.
Preserve preconsumer guards and byte-preservation assertions; only owned synthetic
fixture files are writable. No real process/PID probes, provider/network/account
or private state, shared/runtime edits, installations, commits or releases.

2026-09-08 the migration reviewer has a twelve-minute read-only follow-up on
the newly implemented conversation v2 and process-record version gates. Inspect
current source and the prior owned numeric-version packet; identify actual
old-reader fencing and supported legacy inspection behavior. No tests, shared
edits, private state, providers, accounts, installation, commits or release.
The lead independently reviews accounting admission and final payload binding.

2026-09-08 after F02/L01 closure, two reviewers have twelve minutes each of
read-only remaining acceptance mapping: U01/U02/U03/U04/U07/U08 shell/state/
accessibility/recovery, and L02/L04 format/rollback. Read source, tests and
existing sanitized reports only. No new tests, processes, browser/private-state
access, providers or shared edits. Separate specific runtime defects from missing
rendered/source-bound artifacts, and preserve accepted unchanged boundaries.
E07 work in progress is not a completed schema or migration acceptance claim.

2026-09-08 after accepted R01/R15 packets, two native reviewers have twelve
minutes each of read-only closure crosswalk work: L01/F17 legacy entry and
projection compatibility, and F02 authenticated surface/worker scope. They may
inspect public source, tests and retained planning evidence only. No test runs,
processes, providers, accounts, private runtime state or shared edits. Identify
concrete promised gaps or justified closure; do not invent broader historical
support or OS isolation guarantees beyond the reviewed preview threat model.

2026-09-08 R15 source tracing confirms the existing selected-profile rotation
acceptance gap warrants a bounded synthetic reproduction. Its reviewer may spend
fifteen additional minutes on an owned test-only unchanged/rotated profile pair
at actual resume validation or launch fencing. Reads must be guarded before
consumers and limited to synthetic selected-profile files and public source;
only an exact owned executable may launch. No real account/credential state,
provider, shared/runtime edit, installation, commit or release is authorized.
Historical strict source shapes and candidate Codex status remain unchanged.

2026-09-08 the completed R03/R04 reviewer has ten minutes for a read-only R15
account/profile propagation crosswalk using public source and synthetic tests.
No actual profile/account state, credential resolution, providers, processes,
new tests or shared edits are authorized. Return concrete uncovered promised
paths or existing evidence sufficient for closure, not a generic matrix request.

2026-09-08 final migration audit found no new runtime defect and supports R02
closure after candidate-fork retention. The reviewer has fifteen minutes for
an owned test-only queued registry/adapter/external-version boundary packet,
using existing authenticated synthetic fixtures and exact owned-executable
allowlists. Actual validation and claim writers must remain intact. No shared
or runtime edits, provider calls, accounts, installs, commits or release actions.
The transport reviewer, whose corrected operator packet is independently
accepted, has twelve minutes of read-only R03/R04 source/test crosswalk work.
No processes or tests are authorized for that crosswalk. The conductor remains
the sole shared-runtime integrator and is implementing E07.

2026-09-08 final preview crosswalk grants: the transport reviewer has fifteen
minutes for the remaining operator JSON 4096-byte exact/+1 boundary and fresh/
resume source crosswalk, without repeating accepted transport partitions. Prefer
an in-memory actual authenticated handler; an existing owned loopback fixture
may be used with an exact endpoint allowlist and cleanup, never external network.
The registry reviewer has ten minutes for the candidate-lane actual refusal/fork
table using truthful synthetic history and preconsumer guards. Both may produce
owned portable test-only packets; neither may edit shared/runtime files or access
providers/accounts, install, commit or release.

2026-09-08 a five-minute native test-only follow-up covers the current accounting
producer's optional transient retry through the real synthetic HTTP path and
exact 64/+1 aggregate preparation. It reuses preconsumer guards and owned fixtures,
preserves original acceptance cases, and grants no shared/runtime edits or provider,
account, process, installation, commit or release actions.

2026-09-08 jobs-ordering acceptance grant: after the read-only R01/R02 audit,
the same native reviewer may spend fifteen minutes on an owned, guarded
qualification-revocation versus durable claim-admission regression for gate/main.
Use actual signed synthetic qualification/revocation and claim writers, with a
controlled ordering barrier. Do not fabricate authentication or provider evidence,
edit shared/runtime files, or launch providers. Post-admission revocation is a
separate case from revocation completed before admission. The lead owns ordering
semantics and the conductor owns any required correction.

2026-09-08 the designated conductor reached an authoritative completed turn
after the R12 source correction. The lead resumed it with exact approved test
retention and the reproduced accounting-capacity correction as the next bounded
step; this was not a restart inferred from a timeout. A native helper has a
ten-minute read-only R01/R02 source/test closure audit while those fixes proceed,
with no private state, provider calls, shared edits or broad test execution.

2026-09-08 R12 test-packet grant: the completed source reviewer may spend twenty
minutes creating portable guarded tests in an owned copy for final HTTP bytes,
ACP bytes received by an owned hidden Python process, Gemini file selection and
ArkCLI fresh/resume argv/environment. Prioritize HTTP/ACP if the bounded scope
cannot cover all four. Preserve preconsumer private-state/resolver/network fences;
only the exact owned synthetic child may launch. No shared/runtime edits or new
provider/account/install/commit/release authority. Retain desired failing tests
on a frozen candidate and report their precise boundary; conductor owns fixes.

2026-09-08 native release reviews: one helper has a ten-minute read-only R12
source/test review, with no execution against a moving candidate. A second has
fifteen minutes for E12 actual task/turn roster-drift acceptance in an owned
copy, producing only a portable test packet and source crosswalk. Existing
resolver/process/network/private-state restrictions apply. No shared/runtime
editing or provider, account, installation, commit or release authority is added.
An existing freeze/revalidate fixture alone cannot establish explicit task rebind.

2026-09-08 native follow-ups: the accounting reviewer may spend ten minutes on
owned synthetic grants-producer/adapter consent and repeated-attempt acceptance,
preserving all preconsumer guards and producing test-only evidence. The terminal
reviewer may perform a ten-minute read-only source crosswalk for conversation/
workspace context-policy and accounting integration. No shared/runtime edits,
provider/account access, spend, installation, commit or release is granted.
These are implementation-detail reviews; the lead retains architectural decisions
and the designated conductor retains sole shared-runtime integration ownership.

2026-09-08 bounded native review follow-ups: the terminal-seal reviewer may
freeze current runtime in an owned copy and rerun the unchanged five-case
authenticated regression; completed with five passes and zero guard hits. The
migration/public-boundary reviewer may prepare an owned test-only HTTP accounting
packet using the real request path, a synthetic opener and assertions of durable
prelaunch ordering. Scope is fifteen minutes, no shared edits, no provider/account
access, no installation/commit/release. Credential, process and network guards
remain active; unrelated workspace Git evidence may be replaced with an explicit
synthetic observation. The lead retains integration approval and source review.

Owner correction, 2026-09-06: Summon improvements is the conductor and owns implementation coordination, delegation and workspace/core/UI/test changes. The architect/project lead owns product direction, architectural decisions, priorities, independent evidence review, planning documents and Fable consultation. The conductor acknowledged the current handoff and began execution. Earlier local implementation ownership and unavailable-task observations below are historical; do not treat them as a reason to bypass the designated conductor.

## Operating policy

- The lead owns product direction, architecture, implementation choices, bounded task assignment, progress monitoring, evidence review and keeping the roadmap coherent. Consultation does not transfer this responsibility to the reviewer.
- Fable is the principal consultant and technical second reviewer for consequential plans, unresolved decisions and completed milestones. Ask it to challenge assumptions, look for counterexamples and missing evidence, and distinguish blockers from non-blocking improvements. Bind each endorsement to the reviewed revision and conditions; record the lead's disposition of advice. Exact Fable endorsement requires authoritative served-model evidence; another seat or an inferred identity cannot substitute silently.
- Reviews should be concrete and bounded: state the proposed decision and alternatives, provide the actual diff and verified evidence, identify remaining uncertainty, and ask what would falsify the recommendation. Do not substitute repeated review rounds for implementation progress or manufacture consensus. Previously accepted conditions remain acceptance criteria unless the lead explicitly revises them with documented reasoning and appropriate consultation.
- Agents' ordinary messages remain context. This explicit owner delegation establishes the review role; it does not convert messages into council votes, deliberation ballots, or arbitrary permission changes.
- Preserve pending roster changes, privacy, exact-model gates, ownership fences, provenance and uncertain-spend holds. Keep council and deliberation as separate typed authority kernels beneath the unified workspace.
- Provider calls needed for the requested Fable reviews are authorized. Keep them bounded, use the designated route, and stop on auth, quota, identity or uncertain-spend failure rather than silently retrying or switching accounts.
- General implementation authority does not erase earlier explicit installation, commit, publication/release or credential-change restrictions. Keep those separate from ordinary code/test/documentation work; escalate any proposed change to those boundaries explicitly.
- Ask the owner only when a material scope/authority change or external dependency cannot be resolved through this delegated process. Do not repeat per-file or per-test approval requests.
- Verify each assigned lane is actually running. A message to a completed native helper does not restart it; use an explicit follow-up task when work must resume. Observation timeouts do not justify restarting a live process.

## Active delegation ledger

### E07 authenticated terminal-publication acceptance, 2026-09-08

One native helper may make one isolated test-only attempt of at most fifteen
minutes, without descendants, for the new public/private projection interaction
with governed job terminal sealing. Use the retained candidate and actual
synthetic authenticated job/claim fixtures; no real executor/provider child.
Exercise actual emission and terminal verification without stubbing away the
stored-object digest check or authenticated ledger. Preserve private accounting,
published summaries, exact canonical digest and uncertainty. Guard imports,
private-file/process/network edges before consumers; use only owned temporary
state. Return a portable regression, actual results, source binding and cleanup.
No shared/runtime correction, private-state inspection, installs, commits or
release. The conductor owns the runtime fix and replacement patch.

### E07 public projection and version correction review, 2026-09-08

Review completed: seventeen selected controls pass with zero boundary hits on
the supplied isolated delta, including eleven additional acceptance cases.
The lead confirmed the delta's unrelated R12 rollback; it remains rejected.
One follow-up of at most eight minutes may make the eleven-case portable test
self-contained for canonical execution: retain its pre-consumer-import
synthetic environment and default-deny I/O/process/network fences inside the
test fixture rather than depending on a private review runner. Preserve exact
behavioral assertions and the separate original evidence; no runtime changes.
The lead continues independent accounting/HTTP review while this runs.

One native helper may review the frozen three-file accounting correction for
at most fifteen minutes without descendants. Apply only the supplied delta
to an individually owned current-source copy, never the shared checkout.
Inspect actual stdout/output-file/history/job-status privacy projection and
supported/unsupported background-record mutation. Run a narrowly guarded
synthetic acceptance partition; preserve private evidence before public
projection and unchanged unsupported records. The lead separately reviews
accounting semantics and physical HTTP boundaries. Do not inspect receipts or
private runtime/account/telemetry state. No providers, real backend launches,
shared edits, installation, commits or release. Return exact source/patch
binding, observed test outcomes, limitations and cleanup; no raw hashes/paths.

### R12 exact decoded-text correction acceptance, 2026-09-08

Completed and independently approved: root inspected the corrected audit fence
and all four portable files, confirmed the seven functional methods unchanged,
and passed seven cases in 5.921 seconds against current copied source. All 112
copied runtime files stayed stable; owned fixtures/children are cleaned and no
live handles remain. Exact canonical test retention is conductor work. The
earlier guard findings below are corrected in the accepted replacement packet.

Functional packet completed: seven positive actual-process controls pass with
111 compared files unchanged. Root reviewed all three portable files and found
the retained harness lacks the granted default-deny private-file guard and
installs its Popen allowlist only after runtime imports. One follow-up of at
most eight minutes may retain the exact behavioral cases while adding audit
guards before consumer imports in the owned worker and synthetic dispatcher.
Read only owned packet/interpreter roots, write only owned fixtures, and permit
only the exact owned synthetic child process; deny other process/network edges.
Return a corrected portable packet and one focused rerun, without changing
runtime source or broadening test scope. Functional success is not revoked,
but canonical guarded retention remains pending.

One native helper may make one isolated test-only attempt of at most twelve
minutes, without descendants, against a frozen copy of the conductor's new
internal exact-text intake. Reuse the existing guarded actual-process harness,
replace normalization characterizations with positive decoded-identity checks,
and retain the external prompt-file compatibility control. Only owned synthetic
children are allowed. Return portable test changes, actual execution evidence,
source drift and cleanup. No shared runtime/test/docs edits, providers, private
state, installs, commits or release. A moving source must be reported, not
silently treated as the tested candidate.

### F01 remaining entity evidence audit, 2026-09-08

Completed: eleven source representations are present; no new implementation
defect was established. Twenty-one inspected source/test files remained stable.
No tests, shared edits or live handles. The direct test-evidence gaps below are
the bounded next action, not a requirement to add schemas.

One native helper may perform a read-only audit of at most twelve minutes,
without descendants, of the eleven required entity mappings and their actual
producer/reader tests. Identify concrete unsupported requirement claims,
especially task, worker incarnation and command variants. Preserve the already
accepted Artifact and Review/Decision tests; do not rerun their acceptance or
duplicate the conductor's mapping edits. Return a concise evidence crosswalk,
stable source identities and only requirement-level remaining gaps. No shared
edits, providers, subprocess tests, private-state reads, installs, commits or
release; optional notes may use an individually owned temporary directory.

### F01 strict entity acceptance, 2026-09-08

Completed and independently approved for exact test-only integration: the lead
inspected the complete test/runner and reused runtime fixture and passed all
18 cases in 8.38 seconds against fresh current-source copies. All 27 source and
fixture dependencies match; guard hits and source drift are zero. Owned
runtime fixtures are cleaned and no live handles remain. No runtime defect or
new schema was established. Canonical test/fixture retention remains conductor
integration work; do not substitute a shortened suite.

One native helper may make a single isolated test-only attempt of at most
fifteen minutes, without descendants, for the three audited direct gaps:
missing/future plan schema and duplicate task IDs; malformed registration and
same worker/different instance refusal across reopen; missing/future operator
command source schema at the actual request reader. Retain positive controls
and unchanged authoritative state/journal evidence. Use synthetic owned
fixtures and default-deny private-file/process/network guards before runtime
imports. Return portable tests, results and source drift. No shared/runtime
edits, providers, private-state reads, installs, commits or release. A failed
case is evidence for conductor correction, not permission to weaken assertions.

### F01 artifact correction acceptance, 2026-09-08

Completed and independently approved for integration: the lead reviewed the
portable test and guard and reran all four cases against current copied source.
Nine runtime dependencies match, source drift and guard hits are zero, owned
fixtures are cleaned and no live handles remain. Canonical test retention and
the semantic inventory correction remain conductor integration work.

One native helper may make a single isolated test-only attempt of at most
twelve minutes, without descendants. Use the retained actual-publication
counterexamples against a fresh copy of the conductor's shared artifact-ID
guard. Verify exact-frame replay, refusal of identical/conflicting metadata
under a new frame before append, unchanged journal bytes, and ordinary reopen
versus strict reconstruction parity. Use explicit owned temporary fixture
roots; never derive write authority from source ancestors. Default-deny process,
network and private-file guards apply. No shared/runtime/inventory edits,
providers, account/telemetry-state reads, installs, commits or releases. Return
portable desired-behavior tests, source drift and cleanup for root review.

### R12 council intermediate-process acceptance, 2026-09-08

Completed: eight actual-process/top-level controls pass, including explicit
normalization defect characterizations. Raw-byte transport and bounded CLI
failure terminals are verified; decoded CRLF/leading-BOM identity is not.
The lead independently confirmed intake normalization. No reviewed transport
source drift or live handles remain; an unrelated shared job-resume edit was
reported separately. Correction remains with the conductor.

One native helper may make an isolated test-only attempt of at most fifteen
minutes, without descendants, against the current council prompt-file change.
The current new test replaces `_dispatch_child`; it proves local file plumbing,
not actual process transport. Exercise the real parent dispatch/child-process
boundary using only an owned synthetic dispatcher, default-deny process/network/
credential guards and a synthetic child environment. Cover admitted long context,
complete Unicode/newline content, actual ceiling/refusal behavior and lifecycle
cleanup. Distinguish direct helper probes from the public council journey.
No submitted/shared runtime changes, provider calls, private-state inspection,
installs, commits or release actions. Return a portable test patch, exact scope,
source drift and cleanup; conductor retains correction/integration ownership.

### E07 frozen-packet correction, 2026-09-08

The conductor may explicitly restart its terminal accounting worker for one
bounded correction attempt of at most 25 minutes, without descendants. Resolve
the three retained contact/uncertainty/coverage counterexamples and the actual
secondary-HTTP submission accounting seam described in the updated E07 brief.
Keep the patch isolated; use guarded synthetic boundary tests and return exact
remaining gaps if unfinished. Root independently reviews the changed seams.
No provider, account/private-state inspection, installation, commit or release
authority is added. The prior completed worker is not presumed live.

### R12 release-claimed transport evidence inventory, 2026-09-08

Completed read-only: the source crosswalk and architect disposition are in
`R12_TRANSPORT_CLOSURE.md`. No tests, probes, edits or live handles were created
by this reviewer. The conductor owns the resulting bounded implementation work.

One native reviewer may make a read-only attempt of at most ten minutes,
without descendants, to enumerate the transport paths actually claimed by the
3.5 preview and their existing exact-byte/boundary evidence. Use the adopted
release scope; do not replace it with universal native/live support or exclude
promised preview journeys. Inspect callers, capability selection and existing
regressions; distinguish executed historical evidence from tests inspected now.
Return a short path-to-evidence crosswalk and concrete missing acceptance or
implementation, without editing files, running providers, inspecting private
state, installing, committing or releasing. Root retains scope decisions and
the conductor retains all implementation ownership.

### F01 Review/Decision behavioral evidence, 2026-09-08

Completed: root inspected and independently reran the frozen packet, 33 cases
pass in 1.13 seconds with zero guard hits and no live handles. Twelve new cases
are approved for test-only conductor integration. Synthetic event construction
is the covered producer; actual conductor admission and persisted reopen are
not claimed. This helper grant is closed.

One native helper may make a single isolated test-only attempt of at most
15 minutes, without descendants. Verify the accepted assessment-as-Review and
workspace next-lane Decision mapping through actual strict validation, reducer
and detached projection. Reuse existing meaningful tests; add only missing
producer/reader, conflicting-identity or authority-preservation acceptance.
Copy a bounded dependency closure, install process/network/private-state guards
before imports, use synthetic fixtures and return exact evidence with drift and
cleanup. No shared or submitted-source edits, inventory integration, providers,
credential/account reads, installs, commits or release actions. The conductor
retains artifact correction, mapping edits and all shared integration ownership.

### G02 validated-snapshot correction, 2026-09-08

Completed: 21 focused cases and eight actual worker invocations pass, with
no source drift, guard hits or live handles. Root approves bounded correction
integration after source/predicate review. Actual public-main lifecycle and
integrated retention remain required for whole G02; this helper grant is closed.

The same native reviewer may make one independent attempt of at most ten
minutes against a fresh frozen copy of the conductor's snapshot correction.
Reuse the retained actual replacement, compatibility and deadline controls;
inspect and run the focused test file with existing synthetic guards. Preserve
the old packet, report source drift and distinguish prior 19-case evidence from
the new candidate. No descendants, shared edits, providers, installs, commits
or release actions. Root reviews integration requirements in parallel.

2026-09-08 completion checkpoint: both G02 final correction review and encrypted
numeric-version acceptance helpers completed their bounded attempts. No helper
handles remain live. G02 returned passing compatibility/deadline controls and
one reproduced post-validation snapshot race; correction remains with the
conductor. Encrypted acceptance passed independent lead inspection and a
16-case rerun and is approved for test-only integration with fixed-suite
retention. Neither result grants provider, commit or release authority.

### G02 final correction regression review, 2026-09-08

One native helper may make one independent attempt of at most 12 minutes,
without descendants, against the frozen isolated correction. Run its focused
tests only after verifying synthetic boundaries, and repeat the actual old-
producer/new-reader and deadline/evidence counterexamples from the retained
compatibility packet against the corrected source. Copy into an owned test
area; do not alter the submitted candidate or shared source. Distinguish
passing assertions from characterization output, account for skips explicitly,
and record baseline/drift and resource cleanup. Root reviews the implementation
in parallel. No provider, private-state, install, commit or release authority.

### L02 encrypted pending-state version acceptance, 2026-09-08

One native helper may create one isolated test-only packet in at most 15
minutes, without descendants. Exercise the actual shipped workspace script and
WebCrypto for supported v1, missing/future/wrong-type encrypted record versions,
and incompatible authenticated-data versions. Preserve encrypted bytes,
operation identity and unresolved state; prove no automatic resend or fresh
operation replaces unreadable work. Reuse the accepted Node harness with
synthetic fetch, owned fixtures and restricted child environment. No real
browser/server/provider or installed account/private-state access. No shared
or runtime edits. Record baseline/drift and exact acceptance limits. Conductor
retains integration authority; no installs, commits or release.

### G02 correction compatibility review, 2026-09-08

Completed: seven guarded worker invocations reproduce ordinary historical carry
redispatch, accepted changed round-one evidence and fresh-checkpoint deadline
extension. The actual continuation registry control now passes. Frozen original
and candidate copies and their sources show no drift (226/225 Python files).
No broad suite repeated, no source edits, boundary hits or retained runtime
handles. Conductor received the reusable packet and remaining correction scope.

One native helper may make one independent review/test attempt of at most
12 minutes, without descendants, against the revised isolated council packet.
No shared/candidate edits. Reuse the previous synthetic counterexamples and
inspect fresh pause deadline binding, original round-one payload identity and
legacy ordinary carry compatibility. In particular, prove whether adding
resolved-agent data changes old ordinary stage identities by producing history
with unchanged shared source and reading it with the candidate. Use owned
synthetic temporary fixtures and process/credential guards only. Record source
baseline/drift and return minimal reusable regression evidence, not a broad
repeat of passing suites. No providers, accounts, telemetry-state, installation,
commits or release. Root reviews the historical-claim packet in parallel.

### F01 requirement-to-evidence reconciliation, 2026-09-08

Artifact follow-up completed: three characterization cases reproduce accepted
duplicate publication versus strict reconstruction refusal, with exact-frame
replay as a passing control. Nine runtime files and shared counterparts are
unchanged; guarded boundaries untouched, fixtures cleaned, no live handles.
Root inspected source/test and chose refusal for all new-frame artifact-ID
reuse, preserving original frame idempotence and old journal bytes. Conductor
owns the isolated `_apply_artifact` correction and portable desired-behavior
tests, coordinated with existing coordinator ownership; no L03 weakening.

Read-only reconciliation completed. The exact requirement names eleven entities;
the existing tool also includes resume and turn-budget names. Artifact and
Review/Decision mappings need correction. Root adopts the existing strict
assessment as the preview Review representation, keeping workspace next-lane
decisions separate from council recommendations and fixed-option deliberation.
No new runtime is justified by identifier spelling alone. F01 remains partial.

Follow-up grant: one isolated test-only attempt of at most 12 minutes, no
descendants, to exercise actual artifact publication, ordinary reopen and
strict reconstruction. Cover identical/conflicting artifact identity reuse
with different frame IDs plus same-frame idempotence. Use owned synthetic
fixtures, source baseline/drift and guarded process/credential/network edges;
no shared or runtime edits. Preserve accepted L03 reconstruction behavior.
Return a reusable acceptance packet and demonstrated results. No provider,
account, telemetry-state, installation, commit or release authority is added.

One native read-only helper may make one attempt of at most 12 minutes, without
descendants, to audit F01 exactly against `WORKSPACE_CHECKLIST_V3.md`, the
current 63-row release matrix, both schema inventory tools, and named-entity
source/tests. Determine whether each named entity has a versioned strict schema,
canonical identity and required migration evidence. Do not infer completion
from validator success or expand F01 to every unrelated format merely because
L02 remains open. Identify genuine cross-dependencies explicitly. No edits,
provider/private-state access or broad test reruns. Return a criterion-by-
criterion source-backed disposition and minimal missing evidence. Root owns
any status change; the original acceptance scope cannot be reduced.

### L01/L02 historical job-claim reader acceptance, 2026-09-08

Completed with a test-only isolated packet: helper 15 passes / 3 failures in
2.85 seconds, independently reproduced by root in 3.63 seconds after complete
test/guard review. The three failures concern authenticated malformed scalar
fields, not an authentication bypass. Both exact historical and modern control
paths pass, including no-mutation qualification refusal and public privacy.
The helper's 226 copied/shared source comparison had no drift; synthetic runtime
cleaned and no live handles. Conductor may correct `_job_resume.py` in isolation
with this one new test file and focused adjacent tests, preserving valid shapes
and coordinating ownership with E07 before integration. No authority expansion.

One native helper may produce a test-only isolated acceptance packet in one
attempt of at most 15 minutes, without descendants. Scope is the exact supported
historical claim field sets in `_job_resume.py`, through actual ledger/child
readers, preserving identities and historical bytes while refusing unqualified
launch authority. Inspect current fixtures before choosing the smallest missing
cases; do not rerun the full resume suite. Record baseline and source drift.
No shared or submitted runtime edits. Synthetic fixtures and owned temporary
test files only, with process/dispatch and credential boundaries fenced before
execution. No providers, installed accounts, credentials, telemetry-state,
installation, commits or release. Return actual evidence, uncovered behavior
and a reusable test packet for conductor integration after independent review.

### E07 isolated submission accounting implementation, 2026-09-08

Conductor may allocate one native worker, without descendants, for one initial
implementation attempt of at most 25 minutes in an isolated source copy. Scope
is the approved submission accounting/public summary contract plus ordinary
foreground, background and retry producer/writer integration and synthetic
acceptance from `E07_SUBMISSION_ACCOUNTING_BRIEF.md`. Record exact writable
files and baseline before dispatch; runtime integration remains conductor-only.
Do not edit council, browser, roster, conversation or workspace protocols in
this packet. No providers, accounts, credentials, telemetry-state inspection,
installation, commits or release. Preserve ephemeral foreground storage,
private/public separation, conservative uncertainty and existing durable owner
fences. Return a reviewable contract/implementation packet and explicit gaps;
the time bound is a checkpoint, not permission to reduce required scope. This
independent work may proceed while the G02 corrections remain isolated.

### G02 independent checkpoint acceptance review, 2026-09-08

Completed: 27 tests pass in 2.50 seconds, but four synthetic executions expose
three concrete carry/spend/roster gaps. The lead inspected the reproducer and
results and separately reproduced the deadline extension. Disposition REVISE,
with findings in `G02_CHECKPOINT_ACCEPTANCE_REVIEW.md`. The 225-file baseline
was stable after the CLI revision; synthetic runtime cleaned, no live handles.
The conductor received correction ownership; shared integration is not approved.

One native helper may make one read-only review/test attempt, at most 15 minutes,
without descendants, against the conductor's isolated G02 candidate. Inspect
the nine new and sixteen legacy reader tests, verify synthetic dispatch and
credential isolation before running them, and assess ownership, deadline,
resume bypass, missing evidence and repeated continuation coverage. Temporary
synthetic reproductions are allowed in a separate owned directory; no edits to
shared source or the submitted candidate. No provider, account, credential,
telemetry-state, install, commit or release access. Record baseline/drift and
owned resources. Root independently reviews the implementation and retains the
acceptance decision; conductor remains sole shared-source integrator.

### E07 submission-contract implementation design, 2026-09-08

Design attempt completed with no edits, tests, probes or live handles. The lead
reviewed the concrete attempt projection, jobs writer, builder and chat guard
seams and recorded architectural disposition in the implementation brief.
Foreground persistence was correctly rejected as an inferred extra requirement.
Conversation guard/journal accounting and additive workspace policy/version
fencing are approved design directions, subject to exact implementation and
independent synthetic acceptance. No runtime or provider authority is added.

One native read-only helper may make one design attempt of at most 12 minutes,
without descendants. Use `E07_SUBMISSION_ACCOUNTING_BRIEF.md` and current source
to propose exact producer/writer/reader integration points, additive versioning
and a bounded acceptance partition. Cover each actual submission, scoped
estimates, reported completeness, historical compatibility and per-task
context-policy controls without inventing compaction or weakening unknown
contact/spend. No code edits or provider/account/telemetry-state access. Do not
repeat the finished audit or design a new scheduler. Return one implementable
contract with unresolved architectural choices explicit. Root owns the decision;
conductor remains implementation coordinator. No installs, commits or release.

### L02 browser view-version acceptance, 2026-09-08

Correction completed and independently accepted for integration. The final
36 tests pass in 2.43 seconds, including all 23 actual-script schema/response/
recovery cases and the existing 13 page tests. The lead reviewed the three-file
patch, caught the transient-503 regression, and verified its correction.
Applicability passes, shared baseline is unchanged, and no live handles remain.
Only schema identity and bounded response behavior are established; full v1
shape and rendered-browser acceptance remain separate. Conductor owns the
shared integration and fixed-suite/inventory retention.

Review follow-up grant: one additional isolated attempt of at most 10 minutes,
same three files and restrictions. Before approval, correct a newly introduced
response-order regression: a non-JSON HTTP 503 must retain ordinary bounded
reconnect behavior, not enter permanent schema-compatibility pause. Handle
view 401, then other unsuccessful view statuses, before parsing a successful
view body. Keep response content private. Add actual-script non-JSON/JSON 503
retention and automatic-recovery controls; retain the 21 schema cases and 13
existing page tests. Full valid-v1 projection-shape validation is not established
by this bounded schema-identity correction and remains explicit follow-up.

Acceptance completed with four failures and two supported-schema controls. The
lead independently reproduces all four failures in 0.98 seconds through actual
refresh/request/render JavaScript with real WebCrypto and synthetic fetch. A
missing/future view schema replaces the confirmed view; awaiting-confirmation
operations also lose pending command/message storage and ciphertext and enable
a new action. Already-uncertain records persist, but their confirmed view still
changes. No provider or runtime edits were involved.

Correction grant: one native attempt of at most 20 minutes in the same isolated
copy. Edit `_workspace_page.py`, the retained new schema acceptance file and
directly affected `test_workspace_page.py` fixtures only. Refuse invalid,
missing or unsupported view schemas at every actual incoming-view boundary
before installing pending/current views or reconciling recovery records.
Preserve last confirmed data, pending ciphertext and uncertainty; display a
bounded compatibility error without exposing response content or causing a
request loop. Retain the supported v1 control and verify initial/refresh/poll/
pagination paths as applicable. If existing synthetic views lack a schema,
correct their fixtures rather than treating absent schema as supported. No
other runtime scope, shared edits, providers/account/private-state access,
installs, commits or release. Return the patch, actual affected tests and drift
for independent review before conductor integration.

One native helper may make one acceptance-only attempt of at most 15 minutes,
without descendants, in an isolated source copy. Add one focused test file for
the actual workspace page script receiving missing/unsupported view schemas.
Use local synthetic Node/HTTP fixtures only. Prove whether the response is
refused before changing confirmed view, resolving pending operations or enabling
actions; preserve pending ciphertext and uncertain state. Include a supported
schema control. Do not change runtime or weaken existing fixtures. Return an
actual failing boundary if present, source-bound patch, baseline/drift and owned
handle cleanup. No shared edits, provider/auth/telemetry-state access, installs,
commits or release. Root reviews; conductor integrates.

### F01/L01/L02 format-level inventory audit, 2026-09-08

Implementation complete and independently accepted for conductor integration:
the final 73 metadata/schema tests pass in 1.30 seconds. The lead inspected the
validator, test behavior and sampled producer/reader mappings, correcting the
resume prompt-contract producer to its actual semantic-request/reservation
writers. The final tool reports 52 audited variants, 114 uncovered source/literal
pairs (two candidate G02), 15 manual formats and five missing producer
definitions. Existing entity inventory API remains untouched. Applicability
passes; no shared/runtime edits or live handles. Acceptance proves bounded
metadata behavior, not runtime compatibility or all-format completeness.

Audit complete: the existing validator passes while omitted formats inside
mapped files remain invisible. Numeric and unversioned records are also outside
the literal scanner. Forty-three production schema-bearing files were found
excluding candidate G02; the dated count of 38 is historical. Inspected runtime
coverage is bounded to job/launch/chat/workspace formats, not all formats.

Implementation grant: one native attempt of at most 25 minutes in an isolated
copy, with baseline/drift and no descendants. Add a focused
`tools/format_inventory.py` and `tests/test_format_inventory.py`; preserve the
existing entity inventory API. Define exact versioned format records for the
audited variants, producer/reader symbols, discriminator, authority class and
inspected fixture references or explicit evidence gaps. Report omitted
source/format pairs even when the source file is already mapped; include
explicit numeric/unversioned entries without claiming automatic discovery is
complete. Test a second unknown format in an already-covered source, unsafe
paths, stale symbols/versions, duplicate records and deterministic output.
Resolve paths strictly within the supplied repository root. No runtime edits,
shared writes, providers/spend, private state, installs, commits or release.
Return source-bound patch and focused results for independent review and
conductor integration. Historical claim/view/draft behavior tests remain a
separate follow-up; metadata existence cannot certify runtime behavior.

One native helper may perform one read-only attempt of at most 15 minutes,
without descendants, to enumerate actual durable/public format identities,
including versioned literals, numeric versions and supported unversioned
records. Compare each format against `tools/schema_inventory.py`, identify its
producer/reader and inspected refusal or compatibility fixture, and return
the remaining concrete gaps. A mapped file is not proof that every format in
it is covered. Exclude private on-disk records and provider/account/telemetry
state; inspect repository source and tests only. No edits, providers/spend,
credentials, installs, commits or release. Current incomplete G02 formats must
be labeled candidate-only. The architect owns scope and evidence assessment;
the conductor remains the only shared runtime integrator.

### E07 context-economics evidence audit, 2026-09-08

Parser correction independently accepted for conductor integration: 18 tests
and 20 subtests pass in 1.22 seconds on the reviewed two-file candidate. Shared
baselines match and patch applicability passes. The final fixtures explicitly
fence credential resolution as well as synthetic child execution. An earlier
helper run used three preexisting fixtures without this fence; that batch is
not proven credential-inert and is excluded from the acceptance basis. No
credential inspection was performed to resolve the uncertainty. No live test
handles remain. Raw provider `usage` fallback and broader submission accounting
retain their separate scope; this is not full E07 closure.

Audit complete; source/test behavior inspected without running tests. The lead
independently reproduced OpenCode manufacturing missing input, total, reasoning
and cache-write counters as zero from a synthetic event that reports only output
and cache-read counts. Submission accounting and explicit workspace policy are
separate missing features; existing compiler and whole-message protections stay
accepted at their recorded scopes.

Correction grant: one native attempt of at most 20 minutes in an isolated copy,
with baseline/drift and no descendants. Edit only `_stream.py` and directly
affected stream/OpenCode acceptance tests. Preserve supplied valid numeric
counters, including explicit zero, but do not invent absent counters or infer
totals. Invalid counters must not become measured usage. Keep terminal/served-
model classification and all other backend behavior intact. Exercise partial,
absent and explicit-zero usage/cache through parser and actual synthetic child
publication. Return the patch and actual outcomes for architect review before
conductor integration. No providers/probes/spend, account/auth/telemetry-state
inspection, installs, commits, release or shared edits. Broader contact-certainty
and submission-ledger changes are not included in this grant.

One native helper may perform one read-only attempt of at most 12 minutes,
without descendants, on the E07 requirement and existing source/tests. Identify
actual submission paths, measured versus estimated usage, cache semantics,
per-task opt-out and required-message preservation. Classify existing evidence
against the full requirement and return the smallest justified implementation
or acceptance gaps with source references. Do not declare coverage from test
names alone or read live usage/account/telemetry data. No repository edits,
provider calls/spend, credentials, installs, commits or release. The architect
owns disposition and the conductor remains sole runtime integrator.

### G02 council context implementation checkpoint, 2026-09-08

The conductor's council-only implementation is incomplete. Shared changes to
`_council.py` and new `_council_between_round.py` were made before the required
isolated review. The conductor acknowledged the mismatch and paused shared
writes, preserving the delta without rollback. Work continues in an isolated
copy; local conductor execution must not be labeled a separate native helper.
No test process from its preceding regression remains live. No whole-candidate
capture or G02 acceptance is valid at this checkpoint. Early source review
identified helper import scope, persisted-context validation and synthetic
attempt-accounting defects for correction before public lifecycle tests.

### R01/R02 real background publication acceptance, 2026-09-08

Completed and approved for conductor integration: full source review and
independent execution pass all three actual detached-dispatch cases in 8.12
seconds. The supported PATH shim uses real profile admission. Supported and
mismatched model cases verify immutable bundle identity, terminal/private
publication, public redaction and owned process exit; background `--out`
refuses before detachment. Patch applicability passes. No live handles remain.
This is Windows synthetic publication evidence, not installed-provider or
governed-resume qualification. Integrated execution and fixed-suite retention
remain the conductor's next acceptance steps.

One native helper has an acceptance-only grant: one attempt, at most 15 minutes,
one new test file in an isolated source copy. Exercise actual public background
dispatch through the parent launch, frozen child bundle and terminal/private
publication using only a local marked fake CLI and synthetic profile/roster.
Prefer the supported PATH shim with no pinned profile command, avoiding the
foreground fixture's in-process profile-admission override. Bind the fake child
positively and prevent fallback to any installed provider. Prove terminal source
and observation preservation, public marker exclusion and owned-child cleanup.
Do not fabricate trusted version evidence or a separately authored terminal
receipt. If blocked, retain the actual failing boundary; do not weaken runtime.
No shared edits, other runtime edits, descendants, providers/spend, account/auth/
telemetry inspection, installs, commits or release. Record baseline/drift, exact
checks and all owned handles; stop/clean them at handoff or expiry. The conductor
remains sole integrator and the architect reviews the resulting evidence.

### R01/R02 public emitter acceptance, 2026-09-08

Completed acceptance attempt: the helper retained two failures and one passing
model-mismatch control; independent architect execution reproduces the same
result in 2.51 seconds. The actual executor emits canonical `cli_agent`, while
the continuation writer checks `cli`. Foreground `--out` also serializes the
private launch marker before `_emit` strips it. The architect independently
confirmed the writer leak with synthetic metadata and traced the canonical
driver producer. No live data or provider was used. Relevant source did not
drift; unrelated council integration was preserved.

Correction grant: one native attempt, at most 20 minutes, in the existing
isolated packet. Allowed runtime edits are `_job_continuation.py` and
`run_subagent.py`; allowed test edits are the new publication acceptance file
and directly affected continuation/publication fixture files. Accept the actual
canonical executor route while preserving strict driver/transport/model checks
and historical private sidecar schema. Public output serialization must exclude
private launch evidence without discarding it before private sealing. Do not
change canonical executor taxonomy to fit old fabricated fixtures. Retain valid
legacy sidecar readers and negative route/model controls. No shared-source
edits or other runtime scope; all earlier restrictions and handoff/expiry rules
remain. Return source-bound patch, drift and focused test results for independent
review and conductor integration after the current frozen batch.

Correction complete and approved for integration: independent execution passes
53 continuation/publication cases in 4.43 seconds, including all five real-main
publication cases. The helper reports 113 affected cases before the final two
foreground controls were added. Canonical public intake is strict; historical
private schema remains unchanged. Public `--out` filters a copy and private
sealing retains the original observation. No relevant source drift or live
handles remain. This is synthetic integration evidence, not live qualification.

One native helper may make one isolated acceptance-test attempt, at most 20
minutes, for the missing real source-producer/public-emitter seam. Allowed
edits: one new test file under `skills/summon/scripts` in an isolated source
copy. Use a marked local fake executable, real executor launch observation and
real dispatcher terminal emission/private continuation publication; do not
substitute a separately fabricated terminal receipt for the executor result.
Prove supported publication and refusal/private-marker nonexposure at this
boundary, preserving exact-model and owner/uncertainty semantics. No runtime
edits or broad background/chat matrix expansion in this attempt. If a runtime
defect prevents the journey, retain the failing case and return the precise
boundary for conductor integration. Record baseline/drift and actual outcomes.
No shared writes, descendants, provider/spend, account/credential/telemetry
inspection, installs, commits or release. Stop at handoff, deadline or collision.
The architect owns independent acceptance; the conductor remains sole integrator.

### L01/L02 legacy companion reader check, 2026-09-08

The read-only check completed: the helper reports 16 selected passing tests and
no live handles. The architect independently reproduced council status reporting
consistent after only one generation-counter read with changing observations,
and accepting a foreign-mode/foreign-run receipt for projection. Both synthetic
checks preserved durable bytes. Resume identity remains source-confirmed but
requires a retained refusal case; historical deliberation public-reader coverage
remains separate work.

Follow-up grant: one native implementation attempt, at most 15 minutes, in an
isolated source copy with baseline and drift report. Allowed edits are
`skills/summon/scripts/_council.py` and one focused council-reader acceptance test
file. Correct generation-stable status and receipt mode/run identity validation
for status/resume without changing the existing unversioned format. Preserve
supported carry behavior, ownership fencing and read-only status. Require refusal
before ownership, durable mutation or dispatch for invalid resume identity,
including revalidation under ownership where required by concurrent replacement.
No unrelated council rounds, deliberation, shared-source edits or descendants.
The earlier provider/privacy/install/commit/publication restrictions remain.
Return a patch and actual focused evidence; architect review precedes conductor
integration. Stop at handoff, expiry or source collision.

Completed follow-up: the isolated two-file packet passes 22 helper checks.
Independent architect execution passes 18 cases in 4.94 seconds, including all
16 new reader cases and existing status/full-carry controls. Production source
and relevant test dependencies did not drift; unrelated UI and resume-test work
was observed and preserved. The architect approves this scoped packet for
conductor integration with fixed-suite retention. Post-acquisition replacement
correctly retains the ownership generation advance while refusing journal
repair/carry/dispatch; only pre-acquisition refusals claim byte preservation.
No live handles remain. This does not establish whole L01/L02 or Fable approval.

One native helper has a read-only grant of at most 12 minutes and one attempt
to map council/deliberation companion entrypoints and durable readers to their
actual current/legacy compatibility fixtures. Return at most three concrete
unproven acceptance cases; reuse existing evidence and do not reopen accepted
L03 reconstruction. Focused provider-inert checks are allowed with telemetry,
external pytest plugins, bytecode and pytest cache disabled. No source edits,
descendants, provider/spend, account/credential/telemetry inspection, installs,
commits or publication. Stop at handoff or expiry. The architect reviews findings
and the conductor remains sole shared-source integrator. This review runs beside
the architect's U14 lifecycle checks and the conductor's R01/R02 integration.

### U14 passive-mode and owner-state presentation, 2026-09-08

Current disposition: the native lane completed within its grant with no source
drift or live handles. The architect independently ran all 115 page/view cases
and completed scoped native-browser checks, including connection loss, recovery,
expiry and narrow text layout. The reviewed patch is approved for conductor
integration. `U14_OWNER_PRESENTATION_ACCEPTANCE.md` records the evidence and
remaining integration/lifetime and broader browser-gate limits. The synthetic
browser tab and server are closed; the temporary viewport override was reset.

Bounded native implementation grant: one attempt, at most 20 minutes, isolated
source copy with baseline and final drift report. Allowed edits are
`_workspace_page.py`, `_workspace_view.py` and their existing tests. Expose only
the already validated owner-state enum, keep passive mode visible, and describe
queue evaluation as unavailable on surfaces without an implemented authorized
action. Preserve snapshot freshness and explicit server/work lifetime distinctions;
do not add a daemon, worker launch, new control/authority or speculative command.
Use existing synthetic page/view fixtures and hidden local test launches.

No descendants, shared-source writes, providers/spend, account/credential/telemetry
inspection, installs, commits or publication. The architect reviews the isolated
packet; the conductor remains sole integrator. This independent presentation
slice may run alongside R01/R02 work. Behavioral and rendered acceptance remain
required before whole-U14 acceptance; this grant does not waive either.

### L09 copied-record privacy stages, 2026-09-08

Completed bounded native grants, recorded in the task before dispatch: one
12-minute read-only synthetic consumer review, one 20-minute isolated
implementation and one eight-minute suite-wrapper correction. Each allowed one
attempt and no descendants, shared-source writes, provider calls/spend,
credential/account/telemetry inspection, installation, commits or publication.
The architect reviewed the candidate and the conductor remained sole integrator.
All stages stopped within their grants with no live test handles.

The integrated correction has conductor-reported 121 passing release tests and
independent 86-case Python 3.13 privacy/outcome evidence. This accepts the scoped
copied-record correction only; it is not Fable approval or whole-L09/release
qualification. The conductor continues the existing R01/R02 integration scope;
accepted RB-01 through RB-03, F15 and L03 stay frozen unless relevant evidence
changes. Full release capture waits for the planned candidate, not each patch.

### L02 ordinary swarm compatibility correction, 2026-09-08

Current disposition: completed and integrated. The final isolated regression
reported 119 passes, the conductor's integrated regression reported 119 passes,
and independent shared-source boundary verification passed 42 cases. The final
production/test syntax matches the reviewed packet apart from blank-line
formatting. The broader schema inventory remains L02 work; this does not reopen
the accepted L03 reconstruction contract. The original grant below is historical.

The architect brokers one isolated native implementation attempt, at most
20 minutes, for the six independently reproduced schema/run-identity failures.
Allowed edits: `_swarm_coordinator.py`,
`test_swarm_migration_boundary_acceptance.py`, and narrowly necessary swarm
regressions under `skills/summon/scripts`. Record the current baseline; the
shared checkout is read-only to this helper. Require ordinary readers and
mutators to reject unsupported preparation schema/identity before ownership
or repair, then revalidate the current supported prefix under ownership.
Preserve supported torn-tail recovery, existing ownership fences and accepted
L03 reconstruction behavior. No blanket repair disablement, permissive schema
coercion, provider actions, credentials/telemetry, installs, commits, publication
or descendants. Return a context-aware patch and actual focused evidence;
the conductor alone integrates and should avoid duplicate edits in this packet.
Stop at handoff, expiry or collision. This is implementation authority only;
independent acceptance and required final release review remain outstanding.

### L01 CLI preview designation implementation, 2026-09-08

One native helper may implement the independently confirmed CLI preview-label
gap in an isolated source copy, with a recorded current baseline. Allowed edits:
`skills/summon/scripts/_cli.py`, `_workspace_entry.py`, and focused CLI/entry
acceptance tests only. Add truthful preview designation to workspace help and
create/open/inspect output where applicable, preserving existing commands,
fields, exit behavior, authority and persisted state. Verify actual CLI/entry
behavior with synthetic local fixtures. Return a context-aware patch and evidence;
only the conductor integrates. No shared checkout writes, schema migrations,
providers, credentials, telemetry, installs, commits, publication or descendants.
Allowance: one native-host attempt up to 15 minutes, stopping at handoff, expiry
or collision. This closes only the labeling gap, not all L01 compatibility.

Completed within the grant: four-file isolated patch handed to the conductor;
final copied-source drift check reports none. Architect independently inspected
the patch and passed its three actual CLI/entry tests (6.25 seconds). The helper
has stopped and the grant is expired. Conductor integration and current-tree
verification remain pending; no whole L01 or release acceptance is implied.

### L01/L02 migration acceptance audit, 2026-09-08

The completed L09 native helper may undertake one new read-only assignment,
at most 15 minutes, to compare the exact L01/L02 requirements against the
current schema inventory, legacy CLI/companion-skill/run readers and retained
migration tests. Return concrete missing producer/reader pairs and acceptance
cases, distinguishing actual omissions from stale planning text. This prepares
the conductor's next migration packet while the conductor integrates L09.
No writes, test execution, providers, credentials, telemetry, installs, commits,
publication or descendants. The baseline is the current worktree; report drift.
Stop at the bounded handoff or expiry. This grant permits inspection only;
integration and implementation remain with the conductor.

Completion: the read-only audit returned a source-backed handoff without edits
or test execution; this grant is expired. The architect independently reproduced
the ordinary swarm schema/identity defect in eight retained synthetic cases
(six failures, two passing unknown-event controls) and transferred the new
acceptance file to the conductor. No helper remains assigned to this packet.

The preceding L09 implementation stage is completed and its 25-minute grant
expired. Its 12-file isolated candidate has been handed to the conductor with
the known registry drift and independently isolated pytest-version test defect.

### L09 implementation scheduling grant, 2026-09-08

The conductor may schedule one native implementation helper independently of
the launch-observation correction lane, using the direction in
`L09_SKIP_EVIDENCE_CONTRACT.md`. Record a baseline and use an isolated writable
copy. Allow release outcome/skip collector and policy files under `tools`,
release-tooling tests under `tests`, and explicit skip accounting in the existing
fixed custom test harnesses. Shared production runtime, roster, installation,
CI, planning and other lanes' files are excluded; return any needed changes
outside the grant as findings. The conductor alone reviews and integrates the
patch; the architect independently reviews the new evidence contract.

Allowance is one native-host attempt up to 25 minutes, with no descendants,
provider CLI calls, metered route changes, quota retries, installs, credential
or account access, commits or release actions. Use only synthetic local fixtures
and focused tests. Unknown collection outcomes must remain blocking; do not
weaken or remove release gates. Stop at handoff, expiry or a scope/ownership
conflict. This is a scheduling grant, not a claim that a helper has started;
the conductor must record and verify the actual handle before reporting it live.
Required independent and cross-vendor release review remains outstanding.

Dispatch update: no running L09 implementation handle was reported at the
producer milestone. The architect brokers one native implementation stage
through the completed `l09_skip_contract_review` helper to use its retained
source context. This supersedes the pending conductor dispatch permission:
do not start a duplicate lane. The conductor remains sole integrator and owns
the implementation queue; all path, isolation, time and reserved-action limits
above still apply. The helper may write only its isolated temporary source copy
and patch/handoff artifacts, never the shared checkout. Record its actual
execution state after the follow-up dispatch returns. Native execution is now
confirmed running as `l09_skip_contract_review` in its implementation stage;
the read-only stage below remains a separate completed assignment.

### L09 skip-evidence review lane, 2026-09-08

The architect assigns one native helper a read-only review of the current
`tools/release_gates.py`, `tools/release_manifest.py`, related release tests,
and the L09 coverage requirement. This advances the missing machine-readable
skip inventory while the conductor implements launch observation. The helper
must identify the smallest complete producer/consumer contract and meaningful
acceptance cases, including exact command/source binding, per-test reasons,
platform scope, privacy and refusal of unreviewed skipped mandatory evidence.
It must not edit files, run release gates, install packages, launch providers,
read credentials or telemetry, commit, publish, or spawn descendants.

Allowance: one native-host attempt, at most 15 minutes, no additional provider
route or spend, no quota retry or account switch. Baseline is the current
worktree at dispatch; report any observed source drift rather than claiming
an immutable-candidate review. Completion is a concise source-backed handoff
to the architect; the designated conductor retains all implementation and
integration ownership. The grant expires on handoff or the time limit.
Execution completed: native helper `l09_skip_contract_review` returned a
read-only handoff without tests, writes or provider activity. Its primary
review-file identities remained stable over inspection; the worktree was dirty.
The architect independently confirmed the consumer and custom-harness gaps and
recorded implementation direction in `L09_SKIP_EVIDENCE_CONTRACT.md`. The grant
is expired on handoff; implementation and independent review remain outstanding.

### Bounded independent UI lane, 2026-09-08

The architect assigns scheduling and integration to the designated conductor
for the existing U08 loopback/offline defect. This may run independently of
R01/R02 after the conductor records a single writer and the source baseline.
The permitted implementation files are `_workspace_page.py` and
`test_workspace_page.py` under `skills/summon/scripts`; the retained
`test_workspace_loopback_acceptance.py` supplies independent acceptance and may
receive fixture adaptations only if its behavioral requirements are preserved.
Use an isolated working copy for a writable helper as required by the local
workflow. The conductor alone integrates the returned patch.

Allowance: at most one helper on an already authorized native host route,
one bounded attempt of up to 20 minutes, no descendants, and no new metered
provider route or provider CLI call. Existing account/usage limits stand; do
not retry stopped quota-limited helpers or switch accounts. If no permitted
capacity is available, the conductor may make this small correction at the
next safe edit boundary and then return to chat migration. Do not start another
route. This grant adds no installation, commit, publication,
credential, account, or provider-spend authority. It expires on handoff or the
attempt limit, whichever comes first.

Outcome: reachable loopback requests work despite a browser offline hint;
unavailable-server retries remain bounded; hidden-page and expired/missing-auth
controls still pass. Update misleading offline copy and source assertions as
needed, preserve pending-message uncertainty, and return changed files plus
actual focused test results. Do not redesign the UI or touch runtime, roster,
qualification, or release files. No helper is claimed active until its native
handle is verified. This scheduling grant supersedes only the earlier serial
placement of this small U08 correction after R01/R02; rendered UI acceptance
and every required release gate remain unchanged.

Current execution status: Summon improvements acknowledged conductor ownership and is active on the handed-off runtime/UI milestone. Three former local implementation helpers remain stopped on usage-limit errors. Earlier active/resumed rows describe historical assignments. RELEASE35_IMPLEMENTATION_HANDOFF.md records the preserved source and current handoff; the full goal remains incomplete.

| ID | Owner/seat | Work | State | Evidence/next action |
| --- | --- | --- | --- | --- |
| FABLE-P0-01 | Fable, Claude backend, verified `claude-fable-5-1`, high effort, read-only | Review corrected P0 reliability proposal and authorize bounded implementation/acceptance gates | Completed; approval received | Authoritative reported served identity verified. Nine acceptance conditions; lead found a public-emitter mismatch in F-01 requiring focused correction |
| FABLE-P0-02 | Fable, same verified exact model and read-only boundary | Resolve internal callback versus real CLI envelope distinction in F-01 | Completed; amendment approved | Invocation versus claim scope confirmed. Final conditions recorded in FABLE_P0_APPROVAL.md and sent to developer |
| DEV-P0-01 | Developer task Summon improvements | Implement approved reliability fixtures and contract documentation | Implemented; independent verification passed | Final candidate passes all 189 affected tests; bounded milestone goes to Fable with the Windows evidence |
| LEAD-P0-01 | Project lead | Independently verify diff, test claims, preservation and Fable conditions | Completed | All five findings closed in P0_IMPLEMENTATION_REVIEW.md; 189 passed, no exclusions or source drift during the run |
| FABLE-WIN-01 | Fable, verified exact model | Approve Windows silent-launch correction | Completed; approved | FABLE_WINDOWS_APPROVAL.md records W-01 through W-05 and deferred W-06 |
| DEV-WIN-01 | Developer task Summon improvements | Fix owned Windows launch gaps and add boundary tests/guidance | Implemented; independent verification passed | Final 47-test gate passed after the recorded cold-cache fixture correction; unchanged control suite 60 passed |
| FABLE-WIN-02 | Fable, verified `claude-fable-5-1` | Final P0 and Windows milestone acceptance | Completed; APPROVE | No blocking findings. FABLE_WINDOWS_APPROVAL.md records W-06 and L-01 through L-06 follow-ups; no install/commit/release approval |
| DEV-R02-SPEC-01 | Developer task Summon improvements | Read-only next-migration proposal for R01 remainder, R02 and D06 | Completed; lead corrections applied to proposal | R02_PROPOSAL_REVIEW.md and R02_REFUSAL_CONTRACT.md record exact scope and rejected accounting assumptions |
| FABLE-R02-01 | Fable, verified `claude-fable-5-1` | Approve staged R02 behavior and refusal/UX contract | Completed; CONDITIONAL implementation approval | Stage A may precede qualification; FABLE_R02_APPROVAL.md records A-01 through A-06 |
| DEV-R02-01 | Developer task Summon improvements | Implement canonical chat refusal and explicit-fork behavior with public integrations | Stage A accepted | FABLE-WORKSPACE-01 accepted the correction; independent 63-test/two-subtest final gate. Authenticated qualification and full R01/R02 remain open |
| FABLE-R02-02 | Fable via Summon, verified exact model | Skeptical second review of the frozen Stage A candidate and lead evidence | Completed; CONCERNS, lead REVISE | F-01 independently reproduced on the third continuation. Narrow correction and closure tests recorded in FABLE_R02_STAGE_A_REVIEW.md; prior passing evidence does not establish closure |
| DEV-WIN-02 | Developer task Summon improvements | W06 test subprocess hidden-launch parity and structural guards | Active | Preserve launch semantics and intentional mock negatives; audit actual test subprocess sites before broader verification. External scheduled-window attribution remains separate |
| FABLE-WIN-03 | Fable via Summon, verified exact model | Bounded consultation on popup attribution, caller launch policy and external automation boundary | Completed; scoped plan APPROVE | Shared launch policy verified; external attribution remains unproven and external changes remain outside the assignment. No moving-patch acceptance. Lead deferred further tracing after owner's focus correction |
| WIN-FOCUS-01 | Dedicated Windows subagent | Remaining discovery-test actual launch sites, guard and targeted verification | Completed bounded source follow-up | W06_DISCOVERY_HANDOFF.md records corrected fixture failure, final 16-test gate and limits; lead's updated Windows/structural guard independently passed 13 tests |
| P1-BRIEF-01 | Dedicated workspace design subagent | Concrete first two-worker task/message vertical slice | Completed; lead direction adopted | Includes one-goal/three-lane/two-iteration conductor demonstration and honest authentication limits; source conductor guidance also implemented |
| FABLE-WORKSPACE-01 | Fable via Summon, verified exact model | R02 blocker closure and consequential P1/R13 integration review | Completed; scoped approvals and conditions | FABLE_WORKSPACE_DIRECTION_REVIEW.md records acceptance, five P1 conditions and the durability mechanism requiring independent verification |
| P1-CONTRACT-01 | Dedicated protocol implementation subagent | Pure goal/lane/message/delivery contracts and negative transition fixtures | Implemented; independent preliminary gate passed | 221 tests, no failures/skips/drift. Four lead coherence corrections applied; no runtime activation or authentication proof. Integration review remains |
| R13-BOUNDS-01 | Developer task Summon improvements | Concrete per-record maxima and settlement-obligation count derivation | Completed design probe | Finite reachable-path conservation replaces next-record arithmetic. Constants bind only the final encoded constructors; R13 not complete |
| R13-SEAM-01 | Dedicated storage specialist | Verify uncertain-write, successor and replay mechanism against source | Completed; lead adopted correction | Per-mutation generations and reducer effects disprove blanket tail demotion/duplicate append; strict current-prefix reconciliation is the chosen seam |
| DEV-R13-01 | Developer task Summon improvements | Exact-byte owned append and compatible failure seam, then coordinator reconciliation/reserve | Both increments accepted for Windows provider-inert core | Increment one final gate60 cases; increment two core/layout69 plus separate final capacity/race17 cases, no failures/skips/drift. Actual workspace events, POSIX/power-loss and release qualification remain open |
| P1-REPLAY-01 | Dedicated protocol subagent | Pure coordinator-owned workspace replay and projection | Preliminary helper accepted | All 45 replay tests passed in the corrected independent gate, including linked-delivery grant category; actual coordinator integration remains |
| P1-CONTENT-01 | Dedicated content subagent | Bounded private immutable message content | Preliminary helper accepted | All 24 tests passed in the corrected independent gate. Provision the verified lock before concurrent publication; Windows namespace durability remains unqualified |
| LEAD-P1-R13-01 | Project lead | Independent combined protocol, replay, content, journal and affected-consumer check | Completed; REVISE journal candidate | 383 tests, one failure, no errors/skips/source drift. Artifact publication exposes collision between artifact sha256 and journal checksum. Developer owns correction; no integration acceptance yet |
| LEAD-P1-R13-02 | Project lead | Verify corrected helpers and journal candidate | Completed; journal compatibility condition remains | Corrected scoped gate: 349 JUnit cases, no failures/errors/skips/drift. Old-writer handling of the proposed checksum format remains a separate blocker; passing new-reader tests do not close it |
| P1-TRANSPORT-01 | Dedicated protocol subagent | Bounded authenticated owned-pipe transport and actual fake-worker fixtures | Preliminary helper accepted | Root independently passed 45 Windows tests without failures/skips/drift, including actual children, one-inflight correlated receipts, bounded writes and handle filtering. No grant/provider/hostile-user qualification |
| P1-FIXED-WORK-01 | Dedicated facade/demo subagent | Fixed child computation and separately authenticated result over owned task-bound pipes | Independently accepted as synthetic transport extension | Root passed 59 tests without failures/errors/skips/drift. Three distinct task-bound channels across two logical workers, at most two live; side result remains pending while main result arrives. Supervisor must verify computation before evidence/terminal admission. Durable conductor integration remains open |
| P1-RESULT-01 | Dedicated facade/demo subagent | Supervisor verification of actual fixed-child computation and request scope | Independently accepted as result-verification helper | Root passed 25 tests and 17 subtests, no failures/errors/skips/drift across helper and inspected dependencies. Actual owned-worker receive path, independent computation and task/attempt/context/request binding; receipt-only and authentic-but-wrong results refuse. Evidence proposal only; durable registration/terminal admission await integration |
| P1-CONTEXT-WORK-01 | Dedicated facade/demo subagent | Actual fixed-worker computation over compiled multi-message context and independent supervisor verification | Independently accepted as synthetic helper | Root passed 91 tests and 20 subtests without failures/errors/skips/source drift across the four owned files. Complete selected bytes and independent calculation are checked against actual authenticated child results. Durable conductor integration remains separate; no provider qualification |
| P1-LAYOUT-01 | Dedicated storage subagent | New workspace layout and published-address old-writer refusal | Preliminary helper accepted | Root independently passed 14 Windows tests without failures/skips/drift. Current-class fixtures use six entry-path methods verified unchanged against HEAD; no historical-binary or Linux qualification |
| P1-RUNTIME-01 | Dedicated facade implementation subagent | Coordinator-backed goal/message/delivery facade in separate new files | Independently accepted as gated helper; demo integration preparation active | Root passed 28 JUnit cases without failures/skips/drift. Actual base event support, atomic admission and conductor demo remain open. No parallel base-coordinator editor, raw append or second authority store |
| P1-HOLDS-01 | Protocol/replay subagent and lead | Fable WS-03/05/07/10 bounded schema and replay correction | Independently accepted as pure contracts | Root passed 301 tests without failures/skips/drift: scoped sticky holds, initial-priority task binding, ordered stream prefixes and observation-backed content holds. Actual grant enforcement/integration remains |
| R13-CAPACITY-02 | Independent acceptance-test subagent | Exact capacity boundaries and complete settlement/recovery paths | Independently verified on Windows | Final17 cases include actual two-process last-slot race, exact encoded H±, full cancellation/recovery, constructor bounds and uncertain-fsync replay. No failures/skips/drift; no POSIX/power-loss/P3 claim |
| P1-ADMISSION-01 | Developer task Summon improvements | Actual prospective claim/selection transaction and settlement admission through sole coordinator writer | Active implementation; moving-source review REVISE | Fresh prospective claim and shared selection guard now exist. Corrections assigned: same-transaction workspace/grant authority, canonical composite delivery replay and proof resolution, transaction-owned lease time, and durable duplicate lookup before fresh lifecycle checks. External Fixture snapshots and fixed clocks do not establish integration acceptance |
| P1-ATOMIC-CAPACITY-01 | Dedicated capacity acceptance subagent | Actual composite-admission capacity boundary and settlement fixture | Full frozen fixture independently passed | Root passed 6 tests and 3 subtests without failures/errors/skips/source drift. Includes exact admission/no-half boundaries, newly registered scoped evidence through acknowledgement, held/dead-letter uncertainty, invalid/duplicate slot credit and close refusal until clean no-contact disposition. Acknowledged contact still needs explicit effect resolution before clean close; public facade and operational demo qualification remain separate |
| P1-INTEGRATION-02 | Local protocol/coordinator implementation subagent | Per-delivery atomic proofs, complete consumable lifecycle reserve, unsettled-delivery close guard and truthful facade activation | Active; lead explicitly assigned sole shared-source ownership | External developer issued a final owned-source handoff; two later authoritative task lookups reported no matching task, and schema-message delivery is unconfirmed. The local lane now owns coordinator/admission/state and their existing tests. Runtime and the new capacity test retain separate owners. Stop and reconcile ownership if the external task reappears; no simultaneous shared-file editor |
| P1-ACTIVATION-01 | Local coordinator owner and lead | Centrally enforced public event/atomic capability activation and truthful settlement status | Independently accepted in bounded local scope | Root passed 144 tests and 3 subtests without failures/errors/skips/drift across eight inspected files. Public base advertises actual support; terminal task status cannot conceal unresolved workspace delivery effects. Explicit post-contact resolution and actual conductor/UI acceptance remain separate |
| P1-DEMO-01 | Dedicated facade/demo subagent | Actual public workspace conductor demonstration and fault variants | Normal and bounded fault/restart journeys independently accepted | Root passed 121 tests and 45 subtests after explicit provenance tightening, no failures/errors/skips/drift across ten files. Actual public APIs, three fixed children/max two live, two assessments with side claim active, fresh successor grant, eight actual effect sources and clean close within content bounds. A separate independent demo gate passed 5 tests and 5 subtests without failures/skips/drift, covering bounded fault/restart cases. Supervisor-mediated simulation only; full recovery matrix and worker-origin messaging remain |
| P1-EFFECTS-01 | Protocol reviewer, coordinator owner and facade owner in separate files | Explicit fixed-fixture effect resolution for truthful normal close | Pure/journal and normal actual-child integration independently accepted | Root passed 302 pure protocol tests, separate 102-test/8-subtest effect-capacity/state/admission gate, and the actual-source normal integration within P1-DEMO-01. Counts overlap and are not additive. Separate reservation plus verified owned-child sources preserve contact/ack and simulated provenance; fault/restart qualification continues. No provider or inherited-uncertainty qualification |
| P1-VIEW-01 | Dedicated presentation subagent | Bounded public-redacted and scoped operator presentation contract for task-first shell | Independently accepted as pure presentation helper | Root passed 54 tests with unchanged view/test/protocol/state/admission files. Source review confirms separate task/delivery/effect/closure states, scoped text, opaque cursors, unknown identities and passive defaults. No authentication or UI qualification inferred |
| P1-UI-01 | Dedicated presentation/UI subagent | Actual local task-first page, authenticated read API and coherent snapshot adapter | HTTP/source/view gate independently accepted; rendered review active | Root passed 97 tests with no failures/errors/skips/drift across six owned files. First increment uses real coordinator-backed reads and secret-safe one-time bootstrap. Unsupported controls remain explicitly unavailable; subsequent actual send/disposition/typed review/decision integration and rendered accessibility remain required. No runtime/base/roster edits or provider contact |
| P1-INGRESS-DESIGN-01 | Coordinator implementation subagent | Authenticated worker-origin message and typed CLI/API interface handoff | Complete; contract approved | P1_WORKER_INGRESS_CONTRACT.md records one pipe demultiplexer, channel-derived sender, atomic admission, three authority scopes and exact reserve/replay tests. Design was read-only except its new contract document. Implementation proceeded only after the independent normal-demo freeze was released |
| P1-INGRESS-01 | Coordinator owner, coordinated with runtime/transport owner | Atomic worker-origin send through the existing sole writer | Implementation authorized and active after normal gate release | P1_WORKER_INGRESS_CONTRACT.md is the approved seam. Coordinator/admission/state and focused tests have one owner; runtime/transport remain separate. Three distinct grants, deterministic journal-derived proof, bounded operation index, atomic queued response and exact-cap settlement. No generic raw-event ingress bypass or premature capability claim. Typed supervisor endpoint remains required P1 follow-on |
| FABLE-P1-INTEGRATION-01 | Fable via Summon, verified exact root claude-fable-5-1 | Skeptical review of frozen helpers and conductor integration | Completed; scoped APPROVE | One successful consultation, sixteen reviewed files stable. FABLE_WORKSPACE_DIRECTION_REVIEW.md records WS-01–WS-11 lead dispositions; auxiliary model usage disclosed, moving R13 not accepted |
| FABLE-SUPERVISOR-01 | Fable via Summon, exact model required, high effort, read-only | Challenge proposed taskless supervisor inbox and minimum integration gates | Completed; CONCERNS with additive direction endorsed | Exact authoritative root identity and seven unchanged review files verified. SI-01 through SI-07 and lead late-receipt decision recorded in FABLE_WORKSPACE_DIRECTION_REVIEW.md. Auxiliary model metadata disclosed; no exclusive-model, activation or release claim |
| EFFORT-EVIDENCE-01 | Dedicated source-review subagent | Classify requested-versus-routed Codex effort labeling report | Source triage and immediate documentation correction complete | Lead verified comment/docs now identify requested effort, preserve intentional non-Astra high clamp and explain override ambiguity. Separately derived argument-level receipt metadata remains queued. No provider-served inference or routing expansion |
| FABLE-SUPERVISOR-02 | Fable via Summon, verified exact root model, high effort, read-only | Challenge the frozen accepted supervisor milestone and SI-01 capacity evidence refinement | Completed; scoped APPROVE | One successful attempt, valid report, authoritative matching root identity and eight packet files unchanged. SI02-01 through SI02-05 and lead dispositions recorded in FABLE_WORKSPACE_DIRECTION_REVIEW.md; actual recovery and link-capacity follow-ups assigned. Auxiliary model usage disclosed; no exclusive-model, live/native, UI or release claim |
| P1-OPERATOR-01 | Coordinator/runtime owners and lead | Public authenticated command execution and exact-prefix durable reconciliation | Independently accepted, bounded runtime scope | 74 tests plus 53 subtests, no failures/errors/skips, 31 recursive dependencies unchanged. Exact host-decision source, current authority, uncertain fsync/restart, same-key lookup, queued cancellation and unknown-preserving held disposition verified. Browser integration and human composition remain open; core/runtime owners resumed actual linked-successor recovery |
| P1-OPERATOR-02 | Coordinator/runtime owners and lead | Final append authorization conjunct and Fable recovery-order/capacity follow-ups | Independently accepted, bounded runtime/core scope | 113 tests plus 53 subtests, no failures/errors/skips, 33 recursive dependencies unchanged. Final-sync revocation, post-append reconciliation, same-endpoint cross-sender fencing and real exact-cap link settlement verified. HTTP/browser and actual process recovery remain open; freezes released and owners resumed |
| P1-HUMAN-DESIGN-01 | Core owner coordinated with runtime/UI; lead took local replay ownership after helper stoppage | Explicit local-operator message identity and atomic queued admission handoff | Design accepted; local replay increment tested, full integration incomplete | Lead's combined gate passed 538 JUnit cases including 14 new operator replay cases, no failures/errors/skips/source changes. Composite queue/proof/lookup, identity/order/conflict and structural capacity refusal verified. Dedicated sole-writer/runtime/browser path remains incomplete; draft encryption unimplemented pending review |
| P1-OPERATOR-UI-01 | UI owner and lead | Actual HTTP/browser disposition and durable reconnect/reload | Independently accepted, bounded fixture scope | 172 tests, 35 unchanged dependencies; rendered keyboard cancellation, lost response/full reload/same-origin restart and explicit reconciliation retained uncertainty with two transitions/no duplicates. Narrow layout and focus checked. Owned fixture stopped; human composition/full accessibility/executable launch remain open |
| P1-ENDPOINT-RECOVERY-01 | Runtime owner and lead | Actual fresh Python supervisor endpoint recovery | Independently accepted, simulated scope | Two tests plus two subtests, 22 immutable packet files unchanged; bad source refuses before consumer, new host independently installs authority and performs one linked successor receipt. Parent-harness cleanup provenance explicit; ordinary task recovery/provider/power-loss gates remain open |
| P1-TASK-RECOVERY-01 | Runtime owner and lead | Fresh-process same-task recovery with originally declared attempt budget | Independently accepted, simulated scope | 110 JUnit cases, no failures/errors/skips, 28 packet files unchanged. Queued/included/submitted recovery retains original task/message, explicit fixture confirmation, one successor write and indeterminate/effect history. Current admission helper has later unfinished changes; no combined human-ingress or live-provider qualification |

| P1-HUMAN-CORE-01 | Project lead, local implementation after helper stoppage | Dedicated operator send and synchronized exact-operation lookup through the sole writer | Combined source/test gate passed; runtime/UI incomplete | 242 cases in 137.12 seconds, including 20 real-journal operator cases, no failures/errors/skips or watched-source changes. Current-authority checks, exact-cap cancellation reserve, duplicate/restart lookup and uncertain-fsync/no-repair behavior verified under synthetic host/content callbacks. Actual runtime source/content/browser integration and integrated Fable review remain required |

| CONDUCTOR-RESTORE-01 | Summon improvements; architect/project lead retains review | Verify current human runtime candidate and conduct view/composer/HTTP implementation | Acknowledged and authoritatively in progress | Owner explicitly selected this conductor; complete current-source handoff delivered. Latest runtime candidate passed 13 actual-runtime cases, broader verification remains required. No architect implementation edits after ownership transfer; Fable draft review remains architect-owned and pending |

| FABLE-DRAFT-01 | Fable via Summon; exact root model required, high effort, read-only | Challenge bounded private human-draft reload/restart architecture | Completed; CONDITIONAL, architect design adopted with corrections | One successful attempt in 301.98 seconds, authoritative exact root identity, six unchanged packet files. FABLE_DRAFT_DESIGN_REVIEW.md records eight implementation conditions, stable-operator correction and separate-secret decision. Auxiliary model use and reported private plan artifact disclosed as limitations. Conductor may implement; crypto/HTTP/rendered gates and re-review remain open |

Prior review reports and receipts remain private. Public planning records contain only the lead's sanitized evidence assessment.
# Bounded L02 producer-only classification review

## L02 explicit reader-contract implementation grant

The native helper's F17 read-only challenge has delivered and is closed.
Leadership accepts its producer-only metadata design for a bounded owned-copy
implementation: inventory v2 with explicit `reader_contract` values `required`
and `producer_only_detached`, preserving strict v1 validation. Empty readers
are permitted only for the detached contract with a real producer, private
evidence classification and explicit gap. No fictional reader or runtime change.

The existing native helper may modify only owned temporary copies of
`tools/format_inventory.py` and its metadata tests, returning a patch and concise
evidence. It may copy public source/fixture dependencies needed for source-only
validation. No shared edits, runtime fixture execution/imports, private state,
providers, installations, commits, publication or further delegation. At most
two metadata test attempts, twenty minutes; each hidden child bounded to ninety
seconds, with plugin autoload, bytecode, cache and telemetry disabled. Preserve
all prior cumulative metadata and the pending four-family patch. End on artifact
delivery or a concrete conflict. Conductor alone integrates after lead review.

That read-only follow-up has delivered and its grant is closed. Its proposed
explicit producer-only inventory contract remains advisory, pending a separate
architecture/integration decision; it does not certify a reader exists.

## Bounded F17 contract challenge

Grant the same native helper one read-only challenge of
`F17_RETAIN_LINK_CONTRACT.md` against the named current protocol, state,
admission and runtime seams. Identify at most three material implementation
contradictions or authority/replay gaps, with a concrete correction for each.
No implementation or broader feature review. Read relevant public source only;
no edits, tests, runtime imports, private state, providers or further delegation.
One response, ten minutes, ending on delivery. This is native advisory review,
not Fable approval. The conductor remains sole shared integrator.

Leadership grants the existing native `l02_stable_authority` helper one read-only
follow-up: propose an honest inventory representation for the detached swarm
projection rebuild result. Read only the inventory validator/tests and the
rebuild producer/call sites. Return a concrete minimal metadata design with
negative acceptance cases; do not implement it or invent a production reader.
This advances L02 while the conductor completes F17. No shared or temporary
edits, runtime imports, tests, provider calls, private state reads, further
delegation, installs, commits or release actions. Limit: one response, ten
minutes; authority ends on delivery. The conductor remains sole integrator.

## Fleet launch-evidence reproduction grant

Grant native helper `l02_stable_authority` one bounded reproduction of the
source-traced enriched launch-evidence/exact-field validator mismatch. The prior
metadata grant has ended. This grant permits public source reads and edits/tests
only in a fresh owned temporary copy, with synthetic inputs and no actual process
or provider launch. Trace the executor/control/runtime/claim boundary and test the
same evidence shape against the real validator. Return a minimal regression test,
observed result and proposed correction; do not change shared runtime or integrate.
Disable telemetry, plugin autoload, caches and bytecode; use hidden test children
bounded to ninety seconds. At most two attempts and twenty minutes. No private
state, credentials, network, installs, commits, publication or descendants.
Authority ends on delivery. Preserve strict evidence validation; do not remove
audit fields to make a test pass. Conductor remains sole shared integrator.

The fleet reproduction grant has ended on artifact delivery. Three component
cases were independently replayed successfully; repair remains with the
conductor and is not authorized to this helper by the completed grant.

## U01/U02 read-only preparation grant

Grant existing native helper `l02_stable_authority` one read-only preparation
packet for remaining public review/decision navigation and focal-task artifact
inspection. Read REMAINING_UI_ACCEPTANCE.md and relevant workspace entry, view,
UI, page and adapter source/tests. Return existing capabilities, the minimal
missing integration seams and concrete acceptance cases. No edits, tests,
runtime imports, private state, network/providers, installs, commits, release
or descendants. One response, ten minutes; grant ends on delivery. This is
native advisory preparation, not Fable approval. The conductor retains all
implementation/integration ownership and F17 remains the primary work item.

The U01/U02 preparation grant ended on read-only delivery. The lead checked
the named host constructor, canonical readers, count-only evidence projection
and pure-reader test scope, then forwarded the implementation packet to the
conductor. No implementation or test acceptance follows from that preparation.

## U04 isolated terminal-guidance lane

Grant existing native helper l02_stable_authority one bounded implementation
packet for finite terminal reason projection and capability-derived guidance.
Read REMAINING_UI_ACCEPTANCE.md and relevant workspace protocol/view/page/tests.
Write and test only in a fresh owned temporary source copy; return an additive
patch and exact synthetic regression results. No shared source edits, runtime
provider/worker launch, private state, network, installs, commits, release or
further delegation. Disable telemetry, bytecode and test plugin autoload; hidden
children bounded to ninety seconds, at most three test attempts, twenty minutes.
Preserve protocol/history, authority, unknown exposure and accepted timing.
Conductor alone integrates. Grant expires on packet delivery or a scope conflict.

The U04 isolated lane grant ended on patch delivery. Reported evidence: 137
projection tests and 34 actual-page synthetic DOM assertions. Lead independent
replay and conductor integration remain pending; no shared runtime edits were
made by the helper.

## L07 owned privacy acceptance lane

Grant existing native helper l02_stable_authority one bounded test packet for
L07_OS_PRIVACY_ACCEPTANCE.md. Read relevant public privacy primitives and tests.
Develop/run only owned synthetic temporary fixtures and isolated source copies;
no shared edits or private user-state reads. Native Windows ACL inspection and
ACL changes are permitted solely on verified owned temporary fixture paths,
as are owned junction/hardlink fixtures. Preserve unrelated sentinels; do not
provision accounts, install tools, contact providers/network, commit or release.
Hidden children only; each bounded to ninety seconds, at most three attempts,
twenty minutes, no descendants. Return additive tests, exact results and skips,
with no account identifiers or raw ACL output. Do not claim alternate-principal
access testing. Conductor alone integrates; grant expires on packet or conflict.

L07 source finding: lead confirmed _write_private_token writes before hardening.
The initial lane ended before tests. Renew the same twenty-minute/three-attempt
owned-copy grant, explicitly allowing the minimal _workspace_entry.py writer
repair plus regression tests: protect and verify the new empty file before any
private write, fail closed on protection failure, preserve exclusive-create and
reopen semantics. All earlier synthetic-path, privacy, hidden-process and no
shared-integration restrictions remain. Deliver additive patch to conductor.

The renewed L07 lane delivered an isolated minimal writer repair and eleven new
tests. Reported repaired partition: 33 passed, two deliberately deselected,
zero skips; original-writer negative control fails at the first-write privacy
check. Independent replay is pending. Token reopen through directory reparse
ancestors remains an explicit unverified boundary. The helper grant is closed
on delivery; no shared integration or complete L07 acceptance is implied.

Independent L07 partial replay: repaired writer and content/privacy controls
passed 33 tests in 4.12 seconds with source and fresh copied source unchanged.
Two explicit exclusions remain: worker-capacity and file-symlink tests. Minimal
protection-before-write fix is accepted for conductor integration; no complete
L07 closure. Grant the same helper a final bounded token-reopen junction test
and minimal ancestor-reparse refusal repair, owned copy only. Two attempts,
ten minutes, existing owned-path/hidden-child/privacy restrictions unchanged.
Return additive patch against the preceding packet; no shared integration.

## Scope correction

The conductor reported provider/auth probes and private-state inspection outside
the current provider-inert grant. Leadership explicitly stopped those activities
and redirected work to scoped UI implementation and owned synthetic privacy
integration. No private findings are retained here and no release acceptance
follows from that report. Provider probes, real credential/account/telemetry
inspection and additional spend remain reserved. Request confirmation that any
owned probe process has ended; do not disturb unrelated user processes.

Conductor subsequently confirmed the probe process had exited and no owned
provider/install/release process remained active. It acknowledged return to
provider-inert U01/U02 and exclusion of private health findings from documentation
and release evidence. This confirmation does not retroactively authorize the
out-of-scope actions or qualify a release requirement.

## E12 isolated explicit replacement-binding lane

Grant native helper l02_stable_authority a bounded owned-copy implementation
packet for E12_ROSTER_ACCEPTANCE.md: explicit replacement-name fork binding,
with original history immutable and no implicit launch authority. Read current
conversation/roster seams and existing E12 tests; propose and implement the
smallest explicit typed API consistent with current contracts, returning any
material authority ambiguity to leadership. No shared edits, roster catalog
rewrites, private state, provider probes/launches, network, installs, commits,
release or descendants. Synthetic protocol evidence only, no model certification.
Three hidden test attempts of at most ninety seconds each, twenty minutes;
telemetry/plugin autoload/cache/bytecode disabled. Additive patch and precise
remaining E12 evidence gaps required. Conductor alone integrates after review.

## Local-usage review and telemetry regression lane (2026-09-12)

The user explicitly authorized read-only analysis of local Summon telemetry to
improve 3.5.0. Leadership may aggregate the bounded local event spool; raw records,
private identifiers, paths, prompts and fingerprints must not enter public files
or handoffs. This does not authorize provider/auth probes, credential/account
inspection, additional provider spend, installation, commits or publication.

Grant one native helper a bounded synthetic telemetry producer regression packet:
read current executor/emitter/telemetry source and tests, reproduce fresh inferred
or absent model evidence becoming invalid telemetry, and prepare the minimal
producer-safe correction in a fresh owned copy. No real telemetry/private state
access, shared source edits, providers/network/installs/commits/publication or
further delegation. At most three hidden synthetic test attempts, ninety seconds
per child, twenty minutes; telemetry writes/plugin autoload/cache/bytecode disabled.
Exact-model proof remains fail-closed; disk/caller values cannot become trusted.
Return additive patch and precise before/after results for conductor integration.

The telemetry helper grant ended on delivery after two test attempts. The lead
independently passed its 33-test synthetic producer/audit/gate packet, then
accepted the conductor's integrated producer and timeout normalization with
34 tests in 0.124 seconds. No shared-source drift, bytecode or synthetic-home
state remained. Canonical retention of the new producer test is still required.

## E12 owned packet recovery (2026-09-12)

The previous explicit replacement-binding helper is no longer live. Its owned
source copy exists and contains a partial replacement implementation. Grant one
native helper recovery of that source packet, rather than a parallel redesign.
Read only its base/work source and relevant current public source/tests; do not
inspect private diagnostic, account or home state. Write only to a fresh owned
copy. The conductor alone integrates shared runtime/tests and documentation.

Preserve the explicit replacement-name fork boundary, immutable parent history,
non-launching child, and the historically recorded permission ceiling. Missing
or drifted original catalog entries cannot supply replacement authority. An
unknown historical ceiling must refuse; no current-catalog fallback may invent
it. Preserve frozen definition binding and existing same-name fork behavior.

Return a minimal additive patch, actual current-source compatibility, remaining
E12 acceptance gaps and bounded synthetic evidence. At most two hidden test
attempts of ninety seconds each, twenty minutes; disable telemetry, autoload,
cache and bytecode. No providers, private state, network, installation, commit,
publication, shared edits or descendants. Stop on material authority ambiguity;
this is native engineering evidence, not Fable or live-model certification.

The E12 recovery grant ended on packet delivery after both authorized attempts:
the first lacked pytest before collection; the corrected dependency environment
passed 36 guarded tests. The lead reviewed the additive patch and independently
passed the same 36 cases in 4.850 seconds on fresh current source, with no shared
drift or bytecode. The conductor may integrate this replacement-binding packet;
whole E12 and final candidate gates remain open.
