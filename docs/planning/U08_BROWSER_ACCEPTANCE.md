# U08 scoped reconnect acceptance

Current acceptance, 2026-09-12: U08 is verified for the preview. The new real-
browser writer-loss/recovery test exactly matches the independently executed
bytes and is retained in the fixed workspace_ui command and CI. The integrated
page matches separately accepted U07 text after newline normalization. Earlier
missing-evidence statements below are historical where the
new packet supplies that evidence; actual P2 supervisor handoff remains outside
the passive preview accepted under U14.

Fresh strict-QA replay, 2026-09-14: the retained writer-loss/recovery case
passed 1/1 in 49.04 seconds with the explicit Chromium binding, zero skips,
failures, errors, guard violations, source drift, synthetic-home files or
bytecode. This is current-source provider-inert evidence; it does not change
the separate native mobile-keyboard or assistive-technology limits below.

The lead's fresh current-source copy passed one case in 43.907 seconds, zero
skips/guard violations/source drift/home files/bytecode. Actual journal-writer
release/acquire increases the generation after durable command evidence, and
the unchanged original append raises OwnershipLostError against the stale
writer. The page retains the original operation/body and confirmed stale view;
separate held contact/spend/cleanup remain unknown. No worker or provider runs.
Four natural connection-failed view requests exercise initial jittered retry
growth, one repeated-failure notice, a 30-second next base and successful reset
to two seconds. Existing page-script cap, single-timer, hidden-document and
announcement controls supply complementary evidence. An explicit same-key
lookup returns pending; only explicit continuation finishes that same queued
cancellation. It does not resend uncertain held context or change its effects.

The test is `skills/summon/scripts/test_workspace_recovery_rendered.py` and
depends on the retained canonical rendered fixture. It changes no runtime
source. Actual toolchain is Chromium 149.0.7827.55, Playwright 1.52.0,
Python 3.13.11, Windows 11, headless 1100 by 800. Synthetic screenshots, measured
intervals, exact outcomes and private source binding are retained; the explicit
same-key continuation panel was independently visually reviewed. Passive/
unknown supervisor state remains truthful: this is real writer fencing at a
controlled same-process append boundary, not an active-supervisor lease/status
transition or process death. It does not prove AT speech or mobile keyboards.

The integration worker additionally reports the new recovery case passing in
40.04 seconds, the accessibility case in 13.44 seconds, 78 related page/static
cases and 23 registry checks. Closure rests on independent execution and current
source/test retention; final full candidate evidence remains L09/L12.

Independent architect review, 2026-09-08. This closes the reproduced global
offline-hint suppression defect at its tested scope. It does not complete U08
or approve a release.

## Verified evidence

- Source: `skills/summon/scripts/_workspace_page.py` no longer uses
  `navigator.onLine` to veto local snapshot requests or retry scheduling.
  Authentication and hidden-document scheduling controls remain explicit.
- Independent actual-page-script tests:
  `test_workspace_loopback_acceptance.py`, Python 3.12, initially 5 passed in
  0.45 seconds; expanded acceptance now passes 6 cases in 2.98 seconds.
  Reachable-loopback bootstrap and unavailable-server backoff pass with an
  offline browser hint; hidden-document and expired/missing-auth controls pass.
  The added case executes eight consecutive failures: base delays rise through
  4, 8 and 16 seconds to the 30-second cap, with jitter bounded to 80–120 percent
  (at most 36 seconds). Each failure retains the confirmed view, with one pending
  timer and one unchanged-failure announcement. One hundred scheduling requests
  still leave one timer; hiding the document removes it. A successful request
  restores the 2-second base interval. These are actual page-script measurements
  with simulated requests/timers, not elapsed wall-clock or screen-reader claims.
- Rendered check: Codex in-app browser, default 1280 by 720 viewport, temporary
  provider-free `WorkspaceRuntime` plus real `WorkspaceSurface`, three synthetic
  tasks, ephemeral loopback binding. A synthetic one-time code opened the
  workspace and rendered its confirmed revision. Selecting Task 1 displayed
  pending/claim-none state and explicitly unavailable model evidence.
- After controlled fixture-server shutdown, the rendered page retained the
  selected task, task data and timeline. Connection copy changed to uncertainty
  while the status explained that timeout does not establish worker failure or
  resolve spend. No document-level horizontal overflow was measured.
- The server exited successfully, its temporary directory was removed, and the
  owned browser tab was closed. No provider, production room, account, credential,
  telemetry record or user task was accessed by this fixture.

## Additional rendered checks

Additional rendered acceptance, 2026-09-08: in a new isolated three-task
fixture, opening Evidence summary focused its Close button. Tab and Shift+Tab
retained focus inside the dialog; Escape closed it, returned focus to the
Evidence summary opener, and preserved Task 1 selection and its displayed data.
With the dialog open again, the fixture server explicitly invalidated its
synthetic browser session. The UI closed the dialog, focused the one-time-code
field, and rendered terminal session-expired guidance that preserved uncertainty
about pending context actions. This tested browser session invalidation, not
provider credentials or runtime owner takeover. The owned server exited
successfully, its temporary directory was removed, and the owned tab was closed.

Long-journal acceptance, 2026-09-08: a separate real `WorkspaceRuntime` fixture
registered 205 synthetic evidence events through its authorized event API and
actual content-digest resolver, producing 210 workspace events including setup.
The real `WorkspaceSurface` rendered 200 cards on the first page and 10 on the
second. At a scrolled position on the second page, one further durable event
was registered. The pending-update notice preserved the first visible event,
scroll position and measured offset exactly. Choosing Show latest displayed
revision 211 with 11 cards while preserving the same first visible event and
offset (zero measured pixel delta). Document horizontal overflow was absent.
This checks real journal/projection/server/browser integration with synthetic
data, not a mocked successful view callback. Fixture preparation was slow under
the current local workload; this was not a controlled performance benchmark.
The server exited successfully, temporary history was removed, and the owned
browser tab was closed.

## Remaining acceptance

The browser check did not force a browser offline hint; the page-script tests
cover that condition. These were passive workspaces, not an operator composer,
ownership handoff, full reconnect/re-authentication cycle,
mobile keyboard test or assistive-technology audit. Browser engine/version
metadata and a retained rendered test artifact were not captured. Full U08
stale/indeterminate recovery, owner loss and
rendered backoff/announcement behavior remain in the release acceptance queue.
The broader U01/U02/U03/U04/U07/U14 gates are unchanged.
