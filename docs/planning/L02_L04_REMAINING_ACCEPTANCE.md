# Remaining format and rollback acceptance

Current acceptance, 2026-09-12. L02 and L04 are verified for the preview after
independent evidence and current source/test/CI/fixed-gate retention. Accepted
historical readers, pending-record/AAD versions, workspace namespace refusals,
state preservation and key continuity must remain retained.

Final L02 closure: all nine metadata files are integrated and retained in the
fixed command and CI. The conductor's complete command passed 212 cases in
35.94 seconds, with 185 rows/719 references and zero literal omissions. The lead
verified the final structural test's exact bytes and the metadata module's
complete identical AST statement multiset; the moved independent row-addition
group preserves dependencies and content. The fleet test initially omitted an
accepted base-reader assertion. It is restored; the conductor's exact affected
case passes in 1.66 seconds. Expanding the verified pure local `fleet_reader`
reference makes its complete AST identical to the independently tested file.
No accepted assertion is weakened. Earlier independent 210-plus-two-fixture-
followup evidence and the runtime fleet/E12 fixtures remain retained. This
closes the preview inventory requirement while preserving all recorded gaps.

2026-09-09 release-platform decision: include the composed rollback test beside
the existing migration suite in the fixed migration_rollback gate command.
Windows CI runs it with full history, Node and junction support. The Ubuntu
informational evidence job must report missing Windows qualification as blocked,
not omit the required test or count a skip as a pass. Final candidate evidence
must be collected on a qualified Windows host, or a separately verified future
artifact-binding path; no new cross-host importer is implied. L09/L12 retain
final candidate evidence and reserved commit/publication requirements.

2026-09-09 L04 canonical retention checkpoint: both composed rollback test
files now exist in the shared tree and the lead verified their complete text
matches the independently passed packet exactly. Windows CI/history/Node and
release-command wiring remain to be verified; the 105.117-second rehearsal
does not need an unchanged rerun for this retention check.

## L02 reader correction

Final structural packet accepted for integration, 2026-09-12: 185 rows and 719
source/fixture references include the actual optional conversation-v2 replacement
and exact fleet eight/ten/eleven-field envelopes. The accepted fleet repair and
its runtime fixture are explicit dependencies. The lead's nine-file run passed
210 cases; two schema cases failed because its owned snapshot omitted the public
skill fixture. After adding that unchanged fixture, the two exact cases passed
in 4.076 seconds; no assertion or runtime changes were made. No skips, guard
violations, runtime imports, source drift, home files or bytecode. Preserve the
first failure record. Retain the ninth metadata test in the fixed command/CI,
run the integrated command once and verify packet retention before L02 closure.

2026-09-12 integration retention: all eight shared test files are byte-identical
to the independently accepted 203-case artifact. The 184 metadata rows match
exactly by identity; module differences are statement ordering and whitespace,
with the same complete top-level statements and schema/version. The conductor
reports 203 integrated passes and 23 release-registry passes. Fixed command/CI
retention is present. Only the assigned actual E12 optional replacement and
accepted fleet-envelope structural reconciliation remain before L02 disposition.

2026-09-12 cumulative remainder accepted for integration: the four accepted
authority/reader/context/fleet packets plus corrected workspace and nonworkspace
v2 remainder packets independently pass the full eight-file metadata suite:
203 passed in 41.674 seconds, zero skips/failures, unchanged shared/owned source,
empty synthetic home and no bytecode. There are 184 entries and no unmapped
discovered source/literal pairs; retained evidence gaps and manual formats keep
this distinct from runtime/historical qualification. Detail bodies and swarm
wire frames are private evidence, not public exports or authority. Distinct
deliberation context input/prompt shapes remain separate. A stale test assertion
requiring literal omissions now asserts retained evidence gaps; new-literal
discovery and validation controls remain intact. The conductor must retain all
seven format-inventory test files plus the schema-inventory test in the fixed
schema_inventory command and CI. After E12 integration, map the actual optional
conversation-v2 participant replacement structure before closing L02.

2026-09-09 ten fleet literals accepted as twelve metadata entries, including
three launch-evidence shapes. Lead source review and independent owned six-file
metadata check passed 178 tests in 21.59 seconds with unchanged owned Python.
An unrelated fixture dependency uses the accepted context snapshot after a
mid-edit source capture; this is not validation of current F17 tests. The packet
depends on the context artifact and adds a fleet metadata test requiring CI and
fixed release-command retention. It records a source-traced executor enrichment
versus exact fleet validator mismatch, already sent to the conductor for bounded
provider-free reproduction. Metadata acceptance neither repairs that mismatch
nor qualifies live fleet launch. All helper work for this packet has ended.

2026-09-09 seven context families accepted for integration: eight metadata
entries distinguish inner prompt and outer result shapes under the same output
literal. Private content, local linkage, hash-preimage consumption and digest-only
rollback consumption remain explicit; no restoration or provider provenance is
inferred. Lead source review and independent owned five-file metadata check
passed 173 tests in 17.73 seconds with unchanged owned Python. This additive
packet depends on the accepted reader-contract-v2 artifact. Its new context test
requires CI and fixed release-command retention after conductor integration.

2026-09-09 reader-contract packet accepted for integration: inventory v2 makes
the detached private projection's lack of a production reader explicit, while
retaining strict v1 input validation and normal reader requirements. The lead
reviewed the incremental patch and independently passed 167 metadata tests in
15.65 seconds against its restored owned artifact; watched owned Python stayed
unchanged. This resolves the helper's packaging uncertainty for those bytes.
The packet depends on the pending four stable authority mappings. Retain the
new reader-contract test file in CI and the fixed schema-inventory command.
Canonical integration is pending; no runtime schema, execution authority or
historical provider qualification changes follow from this metadata acceptance.
The frozen v1 envelope predicate test is synthetic compatibility evidence, not
an execution of historical runtime code.

2026-09-09 current integrated metadata checkpoint: the lead independently ran
`test_format_inventory.py`, `test_format_inventory_l02_acceptance.py` and
`test_schema_inventory.py` together: 125 passed in 9.15 seconds. Watched public
Python sources were unchanged. Plugin autoload, bytecode, cache and Summon
telemetry were disabled; the child process was hidden and bounded. CI and the
fixed release schema-inventory command now retain the separate acceptance file.
This supersedes the earlier partial-integration test count below. The four
stable authority mappings were not yet present at this checkpoint, and the
producer-only projection classification and final F17 schemas remain pending.
No referenced runtime fixtures or provider qualification were executed.

2026-09-09 partial canonical integration check: current shared metadata passes
74 source-only tests in 4.49 seconds with watched public Python source unchanged.
Not all accepted test additions are retained yet, so this is reference-consistency
evidence only. Preserve numeric-binding, telemetry, durable, wire and stable
contract tests through integration; a smaller passing collection does not
substitute for the accepted combined test set.

2026-09-09 telemetry metadata increment accepted for integration: config v1,
historical event v1, spool v2 and public event v2. The lead source-reviewed the
actual consumers and independently passed 74 source-only tests in 3.68 seconds,
with owned Python sources unchanged. No telemetry runtime or state was read.
The config reader ignores schema and spool parsing uses numeric equality;
metadata preserves those limitations rather than claiming strict admission.
The public report reader separately checks exact integer versions and shape.
This patch remains separate from the consolidated three-batch artifact below.

2026-09-09 two further metadata packets accepted for integration. Explicit
AST-only set-member binding represents the historical process-v1 reader without
inventing a producer or executing runtime source; the lead passed 107 metadata
tests in 2.63 seconds. Seven usage-family mappings independently passed 73
tests in 2.18 seconds. Both owned Python trees remained unchanged. These are
separate packet checks, not combined integration or runtime acceptance. The
actual public usage export consumer lacks schema-discriminator validation;
that limitation and unclassified public variants remain explicit. The conductor
has one combined patch preserving all three metadata batches. Its helper-owned
combined source-only check passed 107 tests in 3.55 seconds; the lead verified
exact preservation of the accepted additions/removals. Canonical integration
and its final source-only check remain pending.

2026-09-09 metadata increment accepted for integration: historical conversation
room/event v1 readers, fleet approval store/record, and evidence-only activation
candidate. The lead independently passed 73 source-only metadata tests in
2.34 seconds, with owned Python sources unchanged. This does not execute the
referenced runtime fixtures or close L02. The accepted process-v1 representation
above resolves this packet's earlier metadata gap without a runtime change.

2026-09-09 current acceptance: the unchanged approved 28-case packet passes
all 28 cases in 2.26 seconds on a fresh owned copy of current source. The five
previous failures are corrected: invalid remote process versions refuse before
cancellation mutation, and explicit historical-v1 fork creates a structured v2
child lineage with the prompt preserved and all original bytes unchanged. The
source holds the parent append lock and retains its history digest privately.
The exact approved packet is now canonically retained in the test suite, CI
and release command. L02 remains open for finite format mappings and coherent
E07 producer/consumer coverage; do not repeat the accepted integration work.

### Earlier v2 correction checkpoint

The conductor has now implemented exact integer version readers and new room/v2
and process-record/v2 producers, retaining exact v1 readers. Independent source
review confirms the original type-check corrections, but test acceptance is
pending. New v2 room bytes refuse older v1 readers; unchanged historical v1 files
remain subject to the behavior of older binaries. Current-runtime v1 mutation is
refused. Avoid claiming the new fence retroactively changes historical files.

Two remaining source-confirmed cases are assigned: cancellation appends its
request before validating the remote process-record version, and the advertised
legacy fork remedy itself refuses v1 through `ensure_mutable`. Version refusal
must precede cancellation mutation. Explicit migration fork must create a linked
v2 child without mutating the historical parent or contacting a provider.

The adapted owned packet is independently reviewed and run: 23 passed, 5 failed
in 2.65 seconds. All copied runtime sources match current; the sole difference
among 245 compared existing files is an unrelated coordinator test. The failures
are four actual unsupported-version cancellation mutations and explicit legacy
fork refusal. Guards deny real process/network/credential boundaries, substitute
PID observations and constrain writes to owned fixtures. This accepts the
reproduction and passing reader controls, not L02 closure. The portable test is
approved for conductor integration alongside these fixes.

The packet covers future version 99; consistent v1/v2 and mixed
header/event controls; supported v1/v2 process metadata; existing missing/bool/
float refusal and unknown-spend recovery; public legacy inspection; current
mutation refusal; cancellation ordering and actual explicit legacy fork. Tests
using the current `create()` now produce v2 and do not establish v1 coverage
unless their fixture explicitly constructs a consistent historical journal.

### Earlier independently reproduced failures

The lead independently ran the guarded conversation numeric-version packet:
8 failed and 8 passed in 1.15 seconds against five matching current conversation/
policy/accounting modules. Four failures show actual journal open accepts bool
or float `schema_version` in headers/events. Four show actual `recover_turn`
allows missing/future/bool/float process-record versions to reach substituted
liveness/birth-token probes. No OS probe, cancellation or provider ran; all
relevant bytes remained unchanged. Supported integer and explicit dead-process
recovery controls pass, retaining unknown spend.

Correct exact supported-version validation at the actual authority consumer.
Preserve documented read-only legacy inspection rather than turning all generic
record lookup into a mutation path. Unsupported process metadata must refuse
before recovery/cancel decisions or probes. Retain the sixteen desired cases
and explicit unknown-spend control. The conductor owns the correction alongside
conversation/E07 integration. The current source checkpoint above supersedes
the earlier absence of a runtime correction, without claiming test closure.

## L02 finite inventory reconciliation

Current conversation/process producer references now bind the actual v2
constants. The lead independently passes 68 metadata tests in 0.99 seconds.
This verifies source references and validator behavior, not the referenced
runtime fixtures. Supported historical v1 identities still need distinct,
accurate reader mappings in the complete finite inventory.

Add missing stable mappings for workspace/swarm/wire families, fleet approval/
activation, usage stores/checkpoints and numeric telemetry configuration/events,
using actual producers, readers and meaningful existing fixtures. This is source
inspection, never permission to read private telemetry or usage stores. Metadata
omission alone does not prove a runtime defect. G02 is already mapped and the
old candidate-only description is stale.

When E07 is coherent, inventory its actual policy/economics/plan and chat-guard
versions with compatible-reader and unsupported-mutation evidence. A new child
guard alone cannot prevent an older runtime from opening an unchanged room
format and creating a fresh old-style turn. Define and verify the mutable-room/
turn compatibility fence as part of the feature. Do not mark literals accepted
merely because they appear in source.

## L04 composed rollback rehearsal

2026-09-09 current acceptance: the lead independently passed the full owned
composed rehearsal in 105.117 seconds (one test, exit zero). The actual driver
iterates ordinary, Kimi override and shared-junction branches; all three passed
current/historical/current payload replacement, installed-reader provenance,
v1 read/v2 refusal, encrypted unsent/sending/uncertain recovery, key and durable
identity preservation, guard/cleanup and final source-drift assertions. Earlier
setup failures below are superseded by this result. Exact canonical two-file
retention in CI and the release gate remains before L04 closure. Do not rerun
the unchanged heavy rehearsal merely to restate this evidence.

2026-09-09 source availability correction: local Git references contain exactly
one commit titled `Merge pull request #31: Summon 3.4.0`. The lead independently
verified its root `plugin.json` declares `3.4.0` and its tree includes
`install.py`; no matching 3.4.0 tag is present. Resolve and pin that tree in the
owned rehearsal as a local historical source candidate. This is not evidence
of a signed or published artifact, and must not be described as such. A missing
release tag alone no longer blocks the source-level composed rehearsal.

The historical tree has no workspace reader. Its actual
`_conversation.ConversationJournal.open` uses the v1 header validator and
refuses current v2 rooms. Preserve workspace bytes opaquely during downgrade,
then exercise compatible recovery only after restoring the current payload.
Do not invent a historical workspace reader or count a current-source refresh
as a downgrade.

Existing refresh/uninstall and interrupted-backup tests preserve lease, held
uncertainty, IDs, approval-store bytes, usage preferences and telemetry settings.
They do not yet prove selecting a previous artifact while retaining encrypted
pending state and host retention keys. Compose an owned fixture rehearsal:

1. Bind the selected previous source/artifact and current candidate explicitly;
   a refresh of the current payload is not a rollback. Record unavailable
   provenance honestly rather than inventing a release tag.
2. Preserve original IDs, approval generation, uncertainty, ciphertext and
   retention-key identity across the owned source replacement.
3. Exercise the previous supported reader or typed refusal before mutation,
   then compatible recovery without automatic resend, key rotation or repair.
4. Cover distinct supported path branches or document shared-code equivalence:
   ordinary host roots, the Kimi home override and supported shared/junction roots.

Only owned synthetic fixture state is in scope. Actual managed installations,
accounts/providers and final publication remain separately reserved. A complete
provider/account matrix is unnecessary for this format/rollback requirement.

### Bounded implementation handoff

Reuse `MigrationRollbackGateTests` in `tests/test_migration_gate.py`, especially
`test_workspace_lease_and_incompatible_reader_preserve_state`,
`test_phase1_durable_state_survives_refresh_and_uninstall` and
`test_interrupted_swap_restores_previous_owned_tree`. Its fixture/materialization
helpers already capture durable state. Compose them with the actual host-key
reopen checks in `test_workspace_entry.py` and AES-GCM pending-record recovery
in `test_workspace_encrypted_version_acceptance.py` (both under
`skills/summon/scripts/`). These separate existing tests are reuse candidates,
not proof that the composed journey has passed.

In an owned synthetic test destination, select current, then historical, then
current payloads explicitly. Cover encrypted `unsent`, `sending` and `uncertain`
records; retain ciphertext, host/derived key identity, operation/target IDs,
approval generation and held uncertainty across both replacements. Exercise
historical v1 read success and v2 refusal before mutation, then actual current
host recovery without automatic POST/resend, worker launch, key rotation or
repair. No real managed installation or private state is in scope.

Cover ordinary roots, explicit `KIMI_CODE_HOME`, and owned junction aliases
sharing one physical target. The existing `tests/test_install.py::_run` removes
`KIMI_CODE_HOME`, so the override branch needs an explicitly confined harness.
Node/WebCrypto and the junction fixture must be available for their respective
checks; unavailable facilities remain reported gaps, not successful skips.
Hide and clean up owned child processes. This read-only helper handoff was
source-checked by the lead; no new rollback test was executed.

2026-09-09 proposed composed packet is ready for conductor integration and a
guarded owned-fixture attempt. It contains a driver and worker, now checking the
full expected installed skill payload and importing historical/current recovery
readers from that installed tree. Historical seeding alone intentionally uses
the extracted old source. The lead reviewed the complete initial packet and
the installed-reader correction; both proposed Python files pass AST parsing.
No test or installer was executed. Python guards are not an OS sandbox; exact
reviewed synthetic children and Windows ACL APIs remain explicit boundaries.
Passing syntax and source review do not close L04 or prove the rehearsal works.
