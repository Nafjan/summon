# R16/L08 documentation acceptance

Architect acceptance, 2026-09-12: VERIFIED-FOR-3.5 for current preview
documentation. Final candidate privacy, platform, live-provider, installation,
commit and publication gates remain separate.

At the initial acceptance checkpoint, seven integrated documents exactly retained
their independently source-reviewed text: README, PROTOCOL, PHASE1_OPERATOR_GUIDE, CHANGELOG,
ENGINEERING_CHANGELOG, PHASE1_MIGRATION_ROLLBACK and SUMMON_RESUME_CAPABILITIES.
Released changelog history and 3.4.0 product/adapter markers are preserved;
3.5 remains an unpublished workspace preview.

The subsequent bounded public preflight corrections are independently accepted:
three private historical review identifiers are removed while round outcomes
remain; model roster definitions are distinct from live availability; probes
carry explicit possible quota/credit/time consumption; Cursor billing is
labelled Summon's inference; unsupported dated billing/SDK and universal API
compliance claims are replaced with neutral provider/endpoint guidance; and
the roadmap distinguishes the historical preview and GA tags and scopes old
counts to their release. Ten focused source/privacy predicates passed against
the four corrected documents. The actual former identifiers were compared in
memory and were not printed or retained in the review report. No provider,
account, legal-policy or final staged/export qualification is implied.

The later direct-endpoint wording correction is also source-reviewed. README
describes the implemented compatible subset, and the backend reference now
explicitly distinguishes the bundled Anthropic catalog entry from native
Messages-protocol support and requires a compatible gateway. The dispatcher
constructs `/chat/completions`; this review makes no live endpoint or current
provider-availability claim. No additional provider query or unchanged test run
was needed to accept this bounded documentation correction.

The source crosswalk preserves these boundaries:

- Workspace context, dispositions, holds, linked replacements and same-key
  reconciliation are distinct from worker execution and provider authority.
- Continuation declarations do not authorize launch. Certified-lane qualification,
  revocation, queued-version and selected-profile checks remain distinct from
  candidate adapters and unsupported historical profile migration.
- Steering is queued for an eligible later boundary; neither universal native
  session attachment nor live mid-turn delivery is claimed.
- Estimates, reported usage, incomplete coverage and unknown spend remain distinct.
  Usage is advisory; universal quota routing is subsequent work in
  SUBSCRIPTION_FIRST_ROUTING.md.
- Prompt-file input protects the launcher boundary, without making every provider
  transport argv-free. Timeout units, raw versus normalized diagnostic stages,
  foreground server lifetime and explicit authentication repair remain documented.
- Participant replacement freezes the selected definition under the recorded
  permission ceiling. Historical receipts and retired selectors retain identity;
  replacement does not qualify provider continuation.
- Preview acceptance cannot satisfy a blocked publication gate. Inert convergence,
  separately authorized live qualification and final source-bound publication
  are ordered without a circular prerequisite or prose waiver.

The lead independently validated the exact synthetic README plan and public
create/open/inspect parses without starting a host, process or provider. After
the provision-message parser omission was corrected, the lead also passed the
actual public parse-to-dispatch regression alongside the sealed-task alias-remap
control: two cases in 3.529 seconds, zero skips/guard violations, unchanged owned
source and an empty synthetic home. The provision handler is stubbed at its
explicit boundary; this proves reachability, not a live operation. Earlier
retained F17 HTTP/browser evidence remains separate.

The conductor reports the existing operator-guide command check and seven release
contract checks passing; these are not presented as independent lead executions.
Current planning actions and expected-release wording are reconciled against the
authoritative disposition table, preserving historical checkpoints. The Stage A
migration notice is explicitly historical and points to the accepted boundary.

Final changes must preserve this scope and pass the exact candidate documentation
and privacy gate. New exposed capabilities require matching evidence and docs;
these closures do not authorize release.
